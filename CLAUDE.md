# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Communication

The developer communicates in Traditional Chinese (繁體中文) and expects replies in Traditional Chinese by default. Don't switch to English mid-conversation unless explicitly asked to.

## Project Overview

"以太之圓" (The Circle of Ether) — a Godot 4.2 (GL Compatibility renderer) 2D game written in GDScript. The project bundles **two largely independent gameplay systems** in one repo:

- **Main game** — a single-player action game (top-down movement, jump, weapon combat, interact, save stones, bosses).
- **VsMods** — a separate 1v1 versus fighting-game mode (P1/P2 controls, character select, ultimates/skills, stocks/cooldowns).

These two systems do not share a state machine or combat framework — see Architecture below.

## Development

There is no build step, package manager, linter, or test suite in this repo — it's a Godot project edited and run through the Godot 4.2 editor (or `godot` executable/CLI for headless runs). Scenes (`.tscn`) and scripts (`.gd`) are edited together; most gameplay logic lives in `.gd` scripts attached to nodes, so understanding a feature usually requires opening the paired `.tscn` in the editor to see the node tree, not just reading the script.

`project.godot` defines the autoloads, input map, and physics layers — check it when a script references a global (`Game.`, `CombatManager.`, `AudioManager.`, `VsGameManager.`) or an input action (`p1_attack`, `art_1`, `martial_modifier`, etc.) that isn't obviously defined nearby.

## Architecture

### Main game state machine (`classes/`, `player/state/`)

`classes/State.gd` is a minimal contract (`enter()`, `exit()`, `update(delta)`, `physics_update(delta)`, all no-ops by default). `classes/StateMachine.gd` auto-discovers its child `State` nodes into a dict keyed by lowercase node name, injects `player`/`state_machine` back-references, and drives the current state's `update`/`physics_update` each frame. `transition_to(name)` guards against re-entrant transitions. States are wired as child nodes in the Player scene (`player/state/Idle.gd`, `Run.gd`, `Jump.gd`, `Fall.gd`, `Hurt.gd`, `Dying.gd`, `SwapWeapon.gd`, ...).

### Weapon system (`player/`)

`player/Weapon.gd` is the base contract for equippable weapons (`start_light_attack`, `start_heavy_attack`, `get_current_velocity`, `is_attack_finished`, `cancel_attack`, `requires_sheath`, plus a 3-slot "武藝卡帶" special-move system via `martial_slots` / `execute_martial_art(slot)`). Concrete weapons (`Katana.gd`, `Spear.gd`, `Sickle.gd`, `Talisman.gd`) extend `Weapon`.

Combos are **data-table driven, not one-State-per-move**: each weapon defines dictionaries (e.g. `DICT_LIGHT_GROUND`, `DICT_LIGHT_AIR`, `DICT_HEAVY_ULT`) mapping numeric combo-step codes to animation name, hitbox name, damage, knockback, sfx, and action type; an integer `combo_step` drives branching logic in the weapon script itself. Individual `State` subclasses only exist for the broad player states (Idle/Run/Jump/SwapWeapon/...), not for each attack — don't look for a `KatanaAttack1.gd` file, look inside `Katana.gd`'s combo dictionaries.

`MartialArt.gd` is a separate, smaller contract for the special "art" moves (`Art_Katana_1.gd`, etc.) — its own setup/enter/cancel lifecycle, unrelated to `classes/StateMachine.gd`. `ScabbardContainer.gd` handles cosmetic sheath/ghost-trail VFX on weapon swap.

### Input lock (`Player.gd::is_input_locked`)

A single blanket flag meaning "ignore all player input right now," reused for two unrelated purposes with no way to tell them apart from the flag alone: (1) weapon scripts (every weapon's `start_ultimate()`, `Spear.gd`'s `_begin_catch_recovery()` 轉身收槍 前搖) set it during a long committed animation so movement/re-triggering attacks can't interrupt it; (2) `objects/BuildingElevator.gd` sets it for the entire cutscene-like elevator ride as a full freeze — that second case is safe because it also sets `state_machine.process_mode = PROCESS_MODE_DISABLED`, so nothing is left running to consume buffered input anyway.

**Dodge (`slide` action) must always bypass this flag — it's the highest-priority interrupt in the game and is never supposed to be blocked by it.** That exception isn't automatic; it's threaded through by hand at every input-handling site: `Player.gd::_input()`/`_unhandled_input()` both special-case `slide` before the `is_input_locked` early-return/consume, and `WeaponAttack.gd::physics_update()` runs its dodge-cancel check *before* (outside of) the `if not player.is_input_locked:` block, not inside it. If you add a new consumer of `is_input_locked` or a new input-handling entry point, don't assume dodge is automatically exempt — you have to carry the exception over explicitly, the same way these three call sites do.

### VsMods (`VsMods/`)

A fully parallel framework, isolated from `classes/` — do not mix the two. The VsMods was fully rebuilt from scratch; the old character-subclass system no longer exists.

`VsMods/StateMachine/VsStateMachine.gd` extends `Node2D` directly and **auto-discovers child VsState nodes** into a `states` dict keyed by lowercase node name (same discovery pattern as `classes/StateMachine.gd`, but a separate class). `transition_to(name)` has a re-entry guard. A parallel entry point `set_state_quiet(name)` switches `current_state`/`current_state_name` without calling `enter()`/`exit()` — used exclusively during rollback state restoration, never for normal gameplay transitions.

`VsMods/StateMachine/VsState.gd` + `VsPlayerState.gd` — base contracts. `VsPlayerState` provides gravity/move/friction constants and `_apply_gravity(delta)`. `physics_update(delta, input) -> StringName` returns the next state name (empty string = stay); this return-value transition pattern is different from the main game's imperative `transition_to()` calls — don't mix them. Every VsState also exposes `save_state() -> Dictionary` / `restore_state(d)` / `sync_anim()` for rollback; the defaults are no-ops in `VsState.gd`, override only what the state actually needs.

`VsPlayerState`'s `MOVE_SPEED`/`AIR_SPEED` (150/130) and `VsDodge.DODGE_SPEED` (300) are intentionally lower than the main game's `Player.gd` `RUN_SPEED`/dash speed (350/400) — tried matching them 1:1 once, user reverted it (VsMods speed is tuned separately, not meant to mirror the main game's feel). Gravity (980, matches engine default) and jump force (VsMods -420 vs main game -410) are close and not a concern.

`VsMods/player/VsPlayer.gd` (`CharacterBody2D`) — HP/energy/invincibility, `apply_input(delta, input)` driven each frame by `vs_world`. Has `save_state()`/`restore_state()`/`sync_anim_to_state()` for rollback. States live under `VsMods/player/states/` and are scene children of whichever character scene is actually instantiated — see "VsMods 選角色系統" below for why that's `VsPlayerClotty.tscn` (an Inherited Scene), not the flat `VsPlayer.tscn` this paragraph originally described.

Scene flow: `title_screen.gd` → `VsMods/ui/LobbyScreen.tscn` (離線/主機/加入) → `VsMods/ui/CharacterSelectScreen.tscn` (角色選擇，見下) → `VsMods/ui/SelectScreen.tscn` (3 武藝槽選擇) → `VsMods/ui/MapSelectScreen.tscn` (選場地，見「VsMods 選場地系統」) → `VsMods/vs_world.tscn` (戰鬥). `VsGameManager` (autoload, `VsMods/ui/VsGameManager.gd`) caches `p1_arts`/`p2_arts`/`p1_character`/`p2_character`/`selected_arena_id`/`selection_confirmed` across scene changes。

**`VsMods/ui/CharacterSelectScreen.gd`/`.tscn`**（2026-08-03，從 `SelectScreen` 拆出來）——仿格鬥遊戲選角：單一大張角色顯示＋左右箭頭切換（`res://ui/icons/arrow.png`，`TextureButton.flip_h` 做左右鏡像），取代原本擠在 `SelectScreen` 裡的那排小按鈕（新角色 Asatsubaki 加入後，三個角色的按鈕列已經塞不下）。離線模式沿用 `SelectScreen` 原本的 `PanelA`/`PanelB` 左右對半版面邏輯（P1/P2 各自獨立切換），連線模式只顯示本機那半、拉滿全寬。`PortraitAnchor` 目前是空的 `ColorRect` 佔位，留給之後放立繪/角色預覽圖，不強制要求美術才能測整條流程。**角色選擇不需要跟對手比對是否相同**（跟 `MapSelectScreen` 的地圖選擇不同），所以連線模式這裡沒有「等待對方確認」流程，本機選好按確認鍵直接寫入 `VsGameManager.p1_character`/`p2_character` 並前往 `SelectScreen`；對方選了誰，沿用既有機制在 `SelectScreen` 最終確認時透過 `VsNetworkManager.send_arts(arts, character_id)` 一併送出（這個函式本來就已經在送 `character_id`，不用新增 WS 訊息型別，不用動 `signaling_server.py` 白名單）。`SelectScreen.gd` 現在角色是唯讀的（`_ready()` 從 `VsGameManager.p1_character`/`p2_character` 讀入，原本的 `CharacterAnchor`/`_populate_character_row()`/`_on_character_clicked()` 都拿掉了），空出來的高度讓給武藝格。

### VsMods rollback netcode (`VsMods/network/`)

`VsNetworkManager` (autoload) drives frame-locked synchronization:

- **Input delay** — `INPUT_DELAY = 4` is actually applied: `_send_frame` starts at `INPUT_DELAY` while `_game_frame` starts at 0, and frames `0..INPUT_DELAY-1` are pre-filled with empty input `[0,0]` on both sides (in `reset_for_match()`/`_reset()`). This means local input collected on tick N executes on frame N+4, giving remote packets ~67ms head start and cutting prediction depth accordingly. OFFLINE mode bypasses the delay entirely (uses the raw current-tick input).
- **`tick(local_input, cs_frame, checksums) -> Array`** — normally returns `[p1_input, p2_input]`, never stalls. Returns `[]` (empty) during the match-sync waiting phase (see below). If confirmed remote input for the current frame hasn't arrived yet, predicts using `_last_remote_input` (last confirmed remote frame) and records the prediction in `_predicted_remote[frame]`. Predictions are kept until confirmed input arrives — do NOT add early-erase logic here, that was the root cause of a previous "silent permanent desync" bug.
- **Input redundancy** — the DataChannel is unordered/no-retransmit, so a lost packet would otherwise leave that frame's remote input permanently unconfirmed (= permanent silent desync; this was a real bug). Every game packet therefore carries the last `INPUT_REDUNDANCY = 10` frames of input (newest→oldest). `_recv_packet` scans them oldest→newest, storing any not-yet-confirmed frame and setting `_pending_rollback_frame` to the earliest mismatch.
- **`_recv_packet()`** — handles three packet kinds: 4-byte forfeit (`0xFFFFFFFF`), 7-byte sync packet (ready-signal only, carries no real input), and variable-length game packet (min 28 bytes): `[u8 seq][u32 frame][u8 n][n×2B inputs][u32 cs_frame][4×u32 cs]`.
- **Checksums are "settled-frame only"** — comparing checksums of frames simulated with *predicted* input would produce false DESYNC reports, so `vs_world` keeps `_cs_history[frame]` (recorded in `_save_snapshot`, auto-corrected during rollback re-simulation) and each tick sends only the checksum of `VsNetworkManager.get_settled_frame()` (= min(last confirmed remote frame, pending-rollback-start − 1, last simulated frame)), tagged with its explicit frame number. `CS_FAIL_LIMIT = 120` consecutive mismatches (~2s) emits `sync_lost` → both sides independently abort the match to the lobby instead of silently playing divergent realities.
- **`_match_seq`** (1 byte, 0–255) — incremented by `reset_for_match()` each new match. Packets with wrong seq are silently discarded, preventing stale DataChannel-buffered packets from a previous match from polluting the new one.
- **Match-sync waiting** — `reset_for_match()` sets `_waiting_for_sync = true`. While true, `tick()` sends a 7-byte sync packet every 10 physics ticks (~6/s) and returns `[]` so vs_world skips simulation entirely. `_waiting_for_sync` clears when the first valid packet from the remote arrives. This guarantees both sides start from frame 0 simultaneously even if one side entered vs_world much earlier.
- Public API for `vs_world`: `needs_rollback()`, `consume_rollback_frame() -> int`, `get_game_frame() -> int`, `get_confirmed_remote_input(frame) -> InputState`, `get_prediction_depth() -> int` (prediction depth in frames ≈ one-way latency; displayed as ms in BattleHUD).

`vs_world.gd` owns rollback execution:

- Every frame: saves snapshot `_frame_states[frame] = {p1_state, p2_state, round_manager_state, inp1, inp2}` before simulating. Keeps only the last `MAX_ROLLBACK_FRAMES = 20` frames.
- When `needs_rollback()`: `_do_rollback(from_frame, cur_frame, cur_inputs)` — restores the nearest snapshot at or before `from_frame`, re-simulates each frame using `get_confirmed_remote_input()` to override stored remote predictions, then simulates the current frame.
- **`is_resimulating`** flag — set `true` during re-simulation so `_on_round_ended`/`_on_round_started`/`_on_match_ended` signal handlers skip HUD updates.
- After rollback completes, `sync_anim_to_state()` on both players re-syncs `AnimationPlayer` to the restored state.
- **Hit detection** — done exclusively by `_manual_check()` in `_check_manual_hits()`, never via Area2D overlap signals (which don't fire during re-simulation). `VsHitbox.has_hit` is saved in every snapshot to prevent a hit from being re-applied when rollback re-runs the same attack window.
- **Checksums** — `_compute_checksums()` hashes position, velocity, hp/energy/round-manager-state, and player state names into 4 independent u32 values sent with every packet. Divergence is logged as `⚠ DESYNC frame N: 欄位` and shown in BattleHUD. Desync alone does NOT kick players; only exceeding the rollback window triggers `_on_hard_desync()`.

**Signaling server** (`server/signaling_server.py`) — deployed at `wss://the-circle-of-ether-signal.onrender.com` (Render.com free tier, `server/requirements.txt` in subdirectory — Render Build Command must be `pip install -r server/requirements.txt`). Do **not** use `websockets ≥ 14.x` type annotations (`WebSocketServerProtocol` was removed). Room code is currently **1 character** (set in `_new_code()` with `k=1`) for easier testing; change `k` to restore longer codes for production.

**⚠ Redeploying this service can fail with `Timed Out` even though the process starts fine** — confirmed via deploy log showing `Signaling server 啟動，監聽 :PORT` printed successfully, then `Timed Out`. Root cause: Render's health check is a plain HTTP GET, but this server only speaks the WebSocket handshake — without a `process_request` handler, an HTTP request gets rejected by the `websockets` library before ever reaching application code, so the health check never gets a response and Render kills the deploy. Fixed with a `process_request` callback passed to `websockets.serve(..., process_request=_health_check)` that checks `request.headers.get("Upgrade", "").lower() != "websocket"` and only then returns `connection.respond(200, "OK\n")` — **the first version of this fix got that check backwards** (intercepted everything except `/favicon.ico`), which also swallowed real WebSocket handshake attempts and returned HTTP 200 instead of the 101 the WS client expects, breaking all connections outright (Godot's WSL peer errors with `Invalid status code. Got: '200', expected '101'`). `process_request` returning anything other than `None` short-circuits the connection entirely and skips the normal WS handshake — always check whether the incoming request is actually attempting a WS upgrade before intercepting it. This appears to be the **first redeploy since the service's initial setup** — the original deploy apparently never exercised this path, so it's easy to assume redeploys "just work" until you actually trigger one.

**⚠ Message relay is an explicit whitelist, not a generic passthrough** — `handle()`'s `elif t in ("offer", "answer", "ice", "arts", "arena"):` only forwards those five `type` values; anything else is silently dropped (no error, no log). This was previously (incorrectly) described here as "the `\"arts\"` message type is relay-only, server passes it through unchanged" in a way that implied any new `type` would automatically relay — it does not. **Adding a new pre-match message type (like `"arena"` was) requires manually adding it to this tuple**, and since this is a live deployed service, the change only takes effect once the Render service is redeployed — a local edit to `signaling_server.py` alone does nothing for the running server.

### VsMods rollback 確定性規則

Rollback netcode 的正確性完全依賴「兩端從相同初始狀態 + 相同輸入序列，必定算出完全相同的結果」。任何非確定性行為都會造成無法修復的 desync。**在 `_simulate_frame` 的呼叫鏈內（包含 VsState、VsPlayer、VsRoundManager 的所有邏輯），嚴禁以下用法：**

| 禁止 | 原因 |
|---|---|
| `randf()` / `randi()` / `RandomNumberGenerator` 未共享種子 | 兩端產生不同亂數序列 |
| `Time.get_ticks_msec()` / `Time.get_unix_time_from_system()` | 兩台機器的真實時間不同 |
| `get_physics_process_delta_time()` / `Engine.get_physics_interpolation_fraction()` | 實際物理幀時間會因機器效能而漂移 |
| `Engine.time_scale` 影響的 `Timer` 節點 | VsMods 不用 `Engine.time_scale`，但若未來改動要特別注意 |
| Area2D 的 `body_entered` / `area_entered` 信號 | 信號在 `_physics_process` 內不會因幀內幾何計算而觸發，rollback 中間幀偵測不到 |
| `move_and_slide()` / `is_on_floor()` | `move_and_slide` 內部有「上一幀是否在地面」的隱藏記憶（地面吸附用），快照還原無法還原它 → 重模擬的移動結果與直跑不同（實測：一邊多施一次重力，y 永久分歧）。移動一律走 `VsPlayer._move_deterministic()`（無狀態的 `move_and_collide` + 顯式 motion 向量）；地面判定一律讀 `VsPlayer.grounded`（`test_move` 純位置查詢、隨快照保存），states 透過 `VsPlayerState._grounded()` 讀取 |
| `global_position` / `global_transform` | scene tree 的 transform 快取在 rollback 重模擬中不保證更新。模擬邏輯一律用 `position`（vs_world 直屬子節點兩者等值）；hitbox 來源方向用 `VsHitbox.owner_player.position` |
| AnimationPlayer 用真實時間播放 gameplay 軌道 | `VsPlayer.tscn` 的 AnimationPlayer 是 **MANUAL 模式**，由 `apply_input()` 以模擬 delta `advance()` 推進——所以攻擊動畫的 `.:can_combo` / `Graphics/VsHitbox:monitoring` 軌道值是模擬時間的純函數，可以安全參與判定（設計時間點請直接調動畫軌道，別在程式碼裡排程）。`restore_state()` 結尾 `sync_anim_to_state()` 重新對齊動畫時間，重模擬才會重跑軌道 key。若改回 PHYSICS/IDLE 播放模式，軌道值就會脫離模擬時間 → desync。非戰鬥階段（回合結算）玩家不被模擬，`vs_world._simulate_frame` 有純外觀的 advance 防止畫面凍住 |
| Call Method 動畫軌道不加防呆，或 `callback_mode_method` 留在預設值 | 「值」軌道（`can_combo`/`hitbox monitoring`）被 `sync_anim_to_state()` 的 `seek(elapsed, true)` 重複套用是無害的（冪等）；但 Call Method 軌道（例如 `strike_impulse`）每次真的被觸發都會執行一次有副作用的呼叫，不是冪等的。**兩個必要條件缺一不可：**(1) `AnimationMixer.callback_mode_method` 預設是 `ANIMATION_CALLBACK_MODE_METHOD_DEFERRED`（延遲執行）——真正呼叫的時機是 `call_deferred()` 排到這一幀稍後，不是 `advance()`/`seek()` 呼叫的當下；rollback 重模擬同一真實影格內會連續呼叫多次 `advance()`，若都排到延遲佇列，執行次數/順序不保證對應各自的模擬幀，直接破壞確定性——`VsPlayer._ready()` 必須設成 `ANIMATION_CALLBACK_MODE_METHOD_IMMEDIATE`。(2) 即使呼叫已經是即時的，`seek(..., true)` 做 rollback 視覺 resync 時仍可能回放沿途經過的 Call Method key（把已經是正確模擬結果的狀態再蓋一次）——`VsPlayer._resyncing_anim` 旗標防呆（`sync_anim_to_state()` 呼叫期間設 true，方法開頭檢查為 true 就跳過）。實測：只做 (2) 沒做 (1) 完全沒用（guard 早被重置才真的觸發），log 出現 `velocity=(-554.17,0)` 這種只有 `strike_impulse` 直接寫入才會有的異常值。之後新增任何 Call Method 軌道都要同時滿足這兩個條件 |

**正確做法：** 所有模擬邏輯用傳入的 `delta`（在 vs_world 中永遠是 `PHYS_DELTA = 1.0 / 60.0`）驅動。若未來需要亂數（例如角色特技的隨機效果），必須使用兩端共享的固定種子 `RandomNumberGenerator`，種子通過輸入或封包傳遞，不可各自獨立初始化。

### VsMods InputState (`VsMods/network/InputState.gd`)

2 bytes 序列化，所有欄位如下：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `move_dir` | float | −1.0 左 / 0.0 中立 / 1.0 右 |
| `is_crouch` | bool | 蹲下 |
| `jump` | bool | just_pressed |
| `attack` | bool | 普攻 just_pressed |
| `skill` | bool | 技能 just_pressed |
| `art_1/2/3` | bool | 武藝 1/2/3 just_pressed |
| `dodge` | bool | 閃避 just_pressed |
| `guard` | bool | 防禦 pressed（長按） |

**P1 操作特殊**：普攻/技能 = 無修飾左/右鍵；武藝 = E（`martial_modifier`）+ 左/右/中鍵。P2 純鍵盤，無修飾鍵。

**⚠ 連線模式：本機不管佔線上哪個網路身分，操作永遠是 P1 那套配置（A/D+滑鼠），不是依 `VsNetworkManager.local_player_id` 切換讀 `p1_*`/`p2_*`。** `vs_world.gd` 收集本機輸入固定呼叫 `InputState.from_input(1)`（`_physics_process()` 裡 `var local_input := InputState.from_input(1)`，跟 `local_player_id` 無關）；`VsNetworkManager.tick()` 內部才依 `local_player_id` 把這份輸入放進正確的 P1/P2 輸出槽位（`return [li, ri] if local_player_id == 1 else [ri, li]`）。這條規則不是 rollback 戰鬥迴圈專屬——任何畫面（包含純 UI 選單、不受確定性規則約束的地方）只要要讀「本機玩家按了什麼」，一律讀 `p1_*` 動作名稱；只有「這個輸入結果要更新哪一份狀態/顯示在哪個欄位」才依 `local_player_id` 決定。**已經在兩個不同地方因為誤判「這裡不在 rollback 範圍內，可以自己決定要不要套用」而踩過這個坑**（`BattleHud` 的武藝修飾鍵高亮動畫、`MapSelectScreen` 的選場地互動——CLIENT 角色一度改讀 `p2_*` 導致本機按 A/D+滑鼠完全沒反應），判斷這條規則適不適用時，問題永遠不是「在不在 rollback 範圍內」，而是「這是不是本機物理裝置配置」。

### VsMods 體質效果與能量系統（已實作）

- **`VsPlayer.ArmorTier { NONE, HYPER, STRONG_HYPER, INVINCIBLE }`**（2026-07-27 改版：四級、逐招逐時間點可配置，取代舊的「整個狀態型別綁死一種體質」）——`get_armor_tier()` 判斷優先序：(1) `invincible_time_left > 0`（計時器式無敵：回合開局、`VsKnockdown.INVINCIBLE_COVER`、`VsGetup` 起身動畫、`Art_Clotty_1` 反擊窗——這些呼叫端完全不用改，繼續設 `invincible_time_left` 即可）→ `INVINCIBLE`；(2) `VsGuard` 狀態維持原本的雙階段特例（`is_blocking()` 讀 `defense`/`defense_2` 兩支動畫的階段）→ `STRONG_HYPER`/`NONE`；(3) `post_dash_armor_left > 0`（受身 `try_ukemi()` 給的 2 秒強霸體，獨立於「目前放什麼招」的計時器——**改版當下漏保留這條分支，導致受身瞬間又被打中，已修正**，之後任何改動這個函式都要注意這個計時器不是動畫軌道能取代的，因為它可能跨越好幾個不同的狀態/動畫）→ `STRONG_HYPER`；(4) 其餘一律讀 **`VsPlayer.anim_armor_tier`**——由目前播放中的動畫用一條值軌道（NodePath `.:anim_armor_tier`，跟 `.:can_combo` 同一套慣例：discrete `update:1`、逐時間點下 key，收尾記得補一個 key 設回 `NONE`）逐招、甚至同一招不同時間點設定——**`anim_armor_tier` 特意標了 `@export`，`can_combo` 沒有**：Godot 動畫面板的「Add Property Track」選單只列有標記給編輯器看的屬性（`@export` 或引擎內建屬性），純 `var` 不會出現在清單裡，之前 `.:can_combo` 這類軌道其實都是直接手動編輯 `.tres` 文字檔加進去的，不是真的透過編輯器操作——`anim_armor_tier` 因為要讓使用者自己在編輯器裡加軌道，所以標了 `@export` 讓它能出現在「Add Property Track」的屬性清單裡（實測抓到：沒標 `@export` 使用者在編輯器裡完全找不到這個屬性）。之後如果想讓其他純 `var` 也能在編輯器裡直接加軌道，一樣要補 `@export`。，例如「某武藝全程強霸體、戰技全程霸體、普攻只有最後一段給無敵」都是在對應動畫上加關鍵影格，不寫程式碼分支。`anim_armor_tier` 在 `apply_input()` 裡偵測**動畫換了**（`anim_player.current_animation` 跟上一幀不同）的那一刻才重置成 `NONE`，同一支動畫播放期間完全不動它，交給軌道自己的 key 決定何時改變。⚠ **這裡曾經寫成「每幀重置成 NONE 再讓 advance() 重新套用」，是錯的，已修正**：離散值軌道不是「每幀冪等重套當前值」，只有真的跨過關鍵影格那一瞬間才寫入一次——`can_combo`/`monitoring` 能正常運作是因為沒有人每幀主動重置它們，不是軌道自己重套。「每幀重置」會導致只有跨過 t=0 那一幀 armor 是設定值，下一幀重置蓋掉、軌道沒有新 key 可以重新寫入，值就在「設定值／被重置」之間閃爍（實測抓到：`art_2` 的體質輪廓每幀開開關關）。改成「動畫換了才重置」後，沒有軌道的動畫依然會自然落回 `NONE`（換動畫那一刻被重置，之後沒有軌道去改它），有軌道的動畫在整個播放期間值維持穩定，只在軌道自己的 key 上改變。`_armor_reset_anim`（記錄上一幀動畫名稱，判斷是否換了）也要進快照，否則 rollback 重模擬時兩端可能因為這個追蹤值沒對齊而在不同時機重置，是潛在的 desync 來源。**已拿掉「武藝全程至少 HYPER」的地板值**（原本 `cur is VsMartialArt` 特例＋`VsMartialArt.armor_tier_override_strong`，兩者都已移除）——現有 6 招 Clotty 武藝、技能、普攻目前**沒有任何體質保護**（`anim_armor_tier` 預設 `NONE`），要逐招自己在編輯器補 `.:anim_armor_tier` 軌道。`anim_armor_tier` 進快照（`"aat"` key，比照 hitbox `monitoring` 的保守慣例）；`is_invincible()`（純 `invincible_time_left > 0`）維持原樣，只給環境傷害情境用（`take_hazard_damage` bypass、彈簧機關），刻意不受軌道式無敵窗口影響——`_on_hurtbox_hurt()` 的命中無敵判斷改讀 `get_armor_tier() == ArmorTier.INVINCIBLE`（統一涵蓋兩種來源）。體質描邊特效（`_update_status_outline()`）同步簡化成單一 `match get_armor_tier()` 涵蓋四級。
- **`VsHitbox.BreakLevel { NONE, ARMOR_BREAK, STRONG_ARMOR_BREAK }`** — 取代舊的 `guard_break: bool`。判定矩陣在 `VsPlayer._on_hurtbox_hurt()`：STRONG_HYPER 免疫非強破霸的硬直/擊退並 50% 減傷（強破霸攻擊完整生效）；HYPER 免疫非破霸硬直（無減傷）。
- **雙能量池**：`dash_energy`（上限 100，永遠 10/s 回復，衝刺一次耗 30）與 `arts_energy`（上限 50，脫戰 2 秒後 10/s 回復；`out_of_combat_left` 是 float 計時、不用 Timer 節點）。HUD 各畫一條（金色=武藝、青色=衝刺）。
- **衝刺（`VsDodge`）**：期間 `velocity.y = 0`（無重力）。2026-07-27 使用者要求拿掉衝刺的無敵時間與「完美閃避」機制（原本受擊會延伸無敵＋結束後給 2 秒強霸體）——現在純粹是位移＋殘影，被打到就正常進硬直，跟一般移動沒有防禦性質上的差異；`post_dash_armor_left` 欄位保留（受身 `try_ukemi()` 還在用它給 2 秒強霸體），只是不再被 `VsDodge` 寫入。
- **防禦（`VsGuard`）**：即強霸體。普通攻擊 50% 傷害、無擊退硬直；`STRONG_ARMOR_BREAK` 破防 → 完整 `pending_hit` 流程進 VsHurt。
- 受擊方向（擊退的 dir_x）一律從 **`VsHitbox.owner_player.facing_dir`**（攻擊者當下面向）計算，不是兩者相對座標——與主遊戲 `classes/Hitbox.gd` 的 `attacker_dir` 同款慣例，`knockback.x` 為正值 = 「往攻擊者面向那側推開」。`owner_player` 由 `VsPlayer._ready()` 對所有 hitbox 設定，見確定性規則表的 `global_position` 條目（不用 `.position` 是因為算相對座標會依兩人左右位置而正負顛倒——同樣正值卻一下前一下後的 bug 曾發生過，別改回相對座標算法）。

### VsMods rollback 除錯工具

- 每場進 vs_world 會寫 `user://vs_session_p<本機id>.txt`（路徑與設定資訊），DESYNC 事件**即時附加**到 `user://desync_log_p<本機id>.txt` —— 按本機玩家 id 分檔，因為同一台電腦開兩個實例測試時共用 `user://`，不分檔會互相交錯。每場開頭寫 `═══ 場次開始 <時間戳> ═══` 分隔線；附加模式，最上方=最舊。
- desync log 每筆含：偵測當下即時狀態、**分歧幀的封存快照**（雙方 pos/vel/state）、該幀雙方輸入（hex）、預測深度、近期 rollback 記錄（`VsNetworkManager.get_recent_rollbacks()`）。診斷方法：兩台機器對照**同一幀號**的區塊——輸入 hex 不同 = 傳輸層 bug；輸入相同但快照不同 = 模擬層非確定性（可直接從數值差反推，例：vel.y 差 980/60 = 差一次重力）。
- 延遲容量：`INPUT_DELAY 4 + MAX_ROLLBACK_FRAMES 20 ≈ 400ms 單向上限`；超過時 `_do_rollback` 找不到快照 → hard desync 踢回大廳，**這是設計行為不是 bug**（那種延遲下也不可玩）。實測：Clumsy 50ms 整場穩定、零 checksum 分歧；200ms（本機雙實例下實際 ≈400ms）觸發回滾窗保護。

### VsMods 打擊回饋 VFX（震動/火花/殘影）

直接沿用主遊戲 `CombatManager`（autoload，全域可用，不用另外接線）：`apply_camera_shake`、`spawn_spark`、`spawn_ghost`、`spawn_dodge_spark` 四個函式完全通用（不依賴主遊戲的 Player/Enemy 類別），VsMods 直接呼叫即可，沒有重寫任何邏輯。**`CombatManager` 本身就是共用特效庫，不需要另外蓋一個「特效字典場景」**——`VsPlayer.vfx_*` 只是每個角色呼叫它的統一入口，跟主遊戲 `Player.gd` 的 `add_ghost()` 是同一種包裝模式。

- **位置基準**：`VsPlayer.vfx_anchor_position()` 回傳 `Graphics/Sprite2D` 的實際 `global_position`——VsPlayer 根節點對齊腳底（碰撞體/確定性 position 都在腳底），特效要對齊「看起來的身體位置」必須讀 Sprite2D 本身，不能直接用角色的 `global_position`（那是腳底，殘影會貼地錯位，曾經踩過）。`vfx_ghost` 都走這個。
- **火花只綁在命中判定上，沒有動畫軌道版**（`vfx_spark_self` 這種東西刻意不做）：火花是「命中那一刻、命中對象身上」才有的反應，不該是招式動畫自己無條件帶的效果，所以 `vfx_spark` 只由 `vs_world._manual_check()` 在真正判定命中時呼叫，需要一顆實際命中的 `VsHitbox` 才能呼叫（拿不到就沒有簡化版可用）。
- **命中回饋 — 火花的角色預設 + 每招覆寫（比照主遊戲 `Weapon.get_weapon_default_spark()` 三層 fallback，簡化成二選一）**：`VsPlayer` 新增 `default_spark_*` 一組欄位（type/scale/color/aura_color/raw_intensity/random_angle/base_offset/random_offset/attach_to_victim/custom_scene，`@export_group("角色預設火花")`）作為整個角色的預設火花外觀。`VsHitbox` 新增同名的 `spark_*` 欄位＋一個開關 `use_character_default_spark`（**預設 `true`**）：開著時這顆 hitbox 完全套用角色預設，不用碰；想讓某一招火花不一樣才關掉開關、填這顆自己的完整參數。VsMods 是節點驅動（不是主遊戲那種資料表），所以簡化成「整組套角色預設」或「整組用這招自己的」二選一，不像主遊戲能單一欄位分開 fallback——多數情況已經夠用。`shake_intensity` 沒有這層 fallback（跟主遊戲一樣每招自己填，預設 0=不震動）。
  `vs_world._manual_check()` 確認幾何命中後呼叫 `hb_owner.vfx_spark(hb, hrb.global_position, hrb_owner)`／`hb_owner.vfx_shake(hb.shake_intensity)`——**不管防禦/霸體/無敵吸收與否都照樣給回饋**，「打中了」跟「打中造成多少效果」是兩件事，比照主遊戲 `Hitbox._execute_hit()` 的做法，別加條件判斷。`vfx_spark(hb, base_pos, victim)` 內部解析 use_character_default_spark、算隨機角度/偏移（`randf_range`，純視覺兩端各自獨立算不用同步）、決定跟隨目標，最後呼叫 `CombatManager.spawn_spark` 完整參數版——完整複刻主遊戲 `Hitbox._execute_hit()` 的邏輯，不是精簡版。
- **動畫軌道版（震動/殘影，不含火花）**：`vfx_shake(intensity)`／`vfx_ghost(color)` 可以**直接當動畫 Call Method 軌道呼叫**（NodePath 打 `.`、方法名填函式名，跟 `strike_impulse` 同一套用法），適合「這一幀就是要有這個特效」的固定時間點（出招瞬間塵土、蓄力震動等）——這些不是「命中回饋」，是招式本身自帶的效果，所以可以綁動畫時間點，跟火花的定位不同。
- **衝刺殘影**：`VsDodge` 每 `GHOST_INTERVAL`(0.05s) 呼叫一次 `vfx_ghost()`，計時器 `_ghost_timer` 進快照（純視覺但計時器本身要跟著模擬時間走，才不會 rollback 後間隔跑掉）。
- **⚠ rollback 防重複是這套系統的核心約束，兩層防呆缺一不可**：(1) `is_resimulating`（由 `vs_world._simulate_frame()` 每幀同步，**不進快照**——執行期中繼資訊不是模擬狀態）擋掉 rollback 重模擬時的重複觸發；(2) `_resyncing_anim`（`strike_impulse` 那條規則同款）擋掉動畫 resync 的 `seek()` 可能回放 Call Method key。`VsPlayer._vfx_blocked()` 統一檢查這兩者，`vfx_spark`/`vfx_shake`/`vfx_ghost` 內建。**之後任何新增的打擊/招式特效都要走這幾個 `vfx_*` 函式，不要直接呼叫 `CombatManager.*`**，否則會在 rollback 下重複觸發或被 seek 誤觸發。
- **特寫運鏡** — `vfx_closeup(depth, duration)`，可當動畫 Call Method 軌道呼叫。**不是**移植主遊戲 `CombatManager.apply_camera_closeup`（那個直接 tween `camera.zoom` 再還原成固定 `base_zoom`，會跟 `VsCamera` 平常持續動態運算縮放（依兩位玩家距離即時調整 `zoom`）互搶屬性），而是走 `VsCamera`（`VsMods/object/Camera2D.gd`）**自己原本就有、但沒接完的**特寫系統：`cinematic_zoom(target, target_zoom, duration)` 設定 `is_in_cinematic=true` 後，`_physics_process` 會切到專門分支處理鏡頭拉近，不會跟一般立回模式的動態縮放衝突。**修復**：`cinematic_timer` 原本只宣告、從未倒數/檢查，特寫觸發後永遠不會自動結束——已補上倒數邏輯，`duration` 秒後自動退回一般立回模式。
- **⚠ 螢幕震動走 `VsCamera.shake()`，不是 `CombatManager.apply_camera_shake()`**：`VsCamera._physics_process()` 每幀都用自己的 `shake_timer`/`shake_intensity` 決定 `offset`（沒在震動就強制歸零），若改呼叫 `CombatManager` 那邊用 Tween 動 `camera.offset`，兩邊每幀互搶同一個屬性，Tween 效果會被 `VsCamera` 自己「沒在搖就歸零」的邏輯蓋掉，震動等於沒作用（`vfx_shake` 曾經這樣寫，已修正為呼叫 `VsCamera.shake(intensity, duration)`，並比照主遊戲尊重 `Game.config_enable_screen_shake` 設定）。`VsCamera` 早就有自己一套完整的震動系統，別重複做兩套。
- **最後一擊震動**（2026-08-01）：`VsPlayer.FINAL_BLOW_SHAKE_INTENSITY`（18.0）/`FINAL_BLOW_SHAKE_DURATION`（0.25）——比一般命中震動（`hb.shake_intensity`，逐招調，通常小很多）更誇張，只在「這一下正好把 hp 打到 <=0」時額外觸發。因為 `VsCamera.shake()` 是直接覆寫 `intensity`/`timer`、不是疊加，兩個觸發點都要小心順序：(1) 一般攻擊排隊到 `pending_hit`，真正扣血在下一幀 `VsPlayer._apply_pending_hit()`（`was_alive := hp > 0.0` 記錄扣血前是否還活著，避免屍體被連續命中重複觸發）——這一幀跟命中當下（`_manual_check`）的一般震動天生不同幀，不會互相覆寫；(2) 霸體吸收硬直但傷害致命（`_on_hurtbox_hurt()` 的 `not allows_hitstun` 分支）是同幀直接扣血，`vs_world._manual_check()`/`_manual_check_projectile()` 因此在套用完一般命中震動**之後**才補判斷 `was_alive and hrb_owner.hp <= 0.0`，確保最後一擊震動蓋過一般命中震動，而不是被後者蓋掉。
- 刻意跳過的部分（不適合對戰模式）：`CombatManager` 的 domain 時停/`Engine.time_scale`（VsMods 明確禁用）、`spawn_energy_orbs`（主遊戲拾取式能量球，跟 VsMods 自己的雙能量池系統不搭）、`spawn_damage_number`（傷害數字，未要求）。
- **`spawn_anim_vfx(vfx_name, ...)`**（`VsPlayer.gd`，完整移植主遊戲 `Player.gd` 同名函式）— 跟上面 `vfx_*` 系列是不同系統：`vfx_spark`/`vfx_shake`/`vfx_ghost` 是固定打擊回饋（走 `Hitbox.SparkType` 枚舉／`CombatManager` 內建 scene）；`spawn_anim_vfx` 是**字典庫**系統，用 `@export var vfx_common/vfx_weapon/vfx_system: Dictionary`（三個字典，`vfx_name: String → PackedScene`，在 Inspector 展開字典逐一登記）讓任意自訂特效場景都能掛，供招式/武藝專屬視覺使用（不限於打擊）。動畫 Call Method 軌道或程式碼都能直接用字串名稱呼叫，跟主遊戲武器腳本（`Talisman.gd` 的 `player.spawn_anim_vfx("heal_flash", 0, -30)`）完全同一套用法。位置基準是角色 `global_position`（腳底）＋呼叫端自己指定的 `offset_x`/`offset_y`（**不是** `vfx_anchor_position()`——主遊戲的既有特效呼叫本來就是靠 offset 手動校正，換基準點會跟舊的 offset 數值對不上）。同樣受 `_vfx_blocked()` 防呆（`is_resimulating` + `_resyncing_anim`）保護。`VsPlayer.tscn` 的 `vfx_common` 已整批搬入主遊戲 `player.tscn` 原本 `vfx_weapon`+`vfx_system` 的全部 11 個特效（`Circular cut`/`Circular shock wave`/`Cross slash sparks`/`Dimensional Slash`/`Spiral Thrust`/`Thrust`/`blunt_spark`/`chop`/`cut`/`whirlwind`/`Aggregation ring`，場景資源都在 `Explod/tscn/`）——VsMods 目前不分類別，全部塞進 `vfx_common` 一個字典裡，`vfx_weapon`/`vfx_system` 兩個字典留空（@export 還在，之後有需要分類可以用，非必要）。
- **`attack_1~5` 已接上對應的 `spawn_anim_vfx` Call Method key**（從主遊戲 `player.tscn` 的 `Katana/light_1~5` 動畫逐一比對移植，兩邊動畫長度剛好一致：0.5/0.7/0.7/0.6/0.8，時間點可以直接沿用）：attack_1→`"chop"`、attack_2~4→`"cut"`（各自不同的 offset/scale/rotation/color 沿用主遊戲原始數值）、attack_5→`"Thrust"`。觸發時間點對齊到該招**已存在的 `strike_impulse` key**（+0.01s 避免同一條 Call Method 軌道出現重複時間戳——Godot 的 keyframe times 陣列要求嚴格遞增，不能有兩個一樣的時間點），因為使用者自訂的 5 招 `strike_impulse` 時間點本來就幾乎精準對齊各自的 `HitboxAN:monitoring` 開啟時刻（3 招完全相同、2 招極接近），衝力跟火花同一瞬間發生是合理的預設，之後想要拆開時間點直接在動畫面板調 key 即可。

### VsMods 攻擊系統目前狀態（Phase 4）

- **地面普攻** — `VsAttack.gd` 5 段連技，**每招的所有資料都在 `VsPlayer.tscn`，程式碼零資料表**：時間點在 attack_1~5 動畫軌道（`.:can_combo` 軌=連段窗口、`Graphics/HitboxAN:monitoring` 軌=判定框開關，與主遊戲 player.tscn 同款做法）；判定框大小/位置/傷害/硬直/擊退在**每招一顆的 `HitboxA1~A5` 節點**（VsHitbox @export，編輯器直接調）。`VsPlayer.hitboxes` 收集 Graphics 下所有 VsHitbox，快照統一保存各框 `[monitoring, has_hit]`，`vs_world._check_manual_hits` 迭代全部。0.2s 攻擊輸入緩衝（`ATTACK_BUFFER`，必須小於各段窗口開啟時間）；窗口開啟＋有緩衝→立刻取消剩餘動畫接下一段。打斷優先級：衝刺（任何時點）> 防禦（地面限定）> 武藝（未實作，接入點已留）> 連段派生。`combo_step` 在 `exit()` 才重置；連段繼續直接呼叫 `enter()` 繞過防重入鎖。
- `VsAttack.IMPULSE_FRICTION = 8750.0`（比照主遊戲 `Katana.gd` 的 `FLOOR_ACCELERATION = RUN_SPEED/0.04`）是攻擊狀態專用的水平減速率，跟 `VsPlayerState.FRICTION`（900，一般移動用）是分開的兩個常數，別搞混。`strike_impulse` 打出的前衝力道要靠這麼強的摩擦力才煞得住——用一般移動摩擦力的話同樣衝力強度會飛遠將近 20 倍（滑行距離 ∝ v²/摩擦力），這是把主遊戲的 strike_impulse 力道數值直接套用到 VsMods 卻飛超遠的根因，不是 strike_impulse 本身的問題。
- **`strike_impulse(strength)`**（`VsPlayer.gd`，主遊戲 `Player.strike_impulse` 同款設計）— 給動畫 Call Method 軌道呼叫用，在攻擊動畫的前衝/突刺幀把 `velocity.x` 直接設成 `facing_dir * strength`，取代殘留動量；只在 `state_machine.current_state is VsAttack or VsAirAttack or VsSkill or VsMartialArt` 時生效（白名單，四類會用到前衝力道的攻擊性狀態都要列進去——地面普攻先做時只寫了 `VsAttack`，後來加空中普攻/技能/武藝各自的 Call Method 軌道時才發現這裡沒跟著擴充，動畫軌道呼叫全部靜默 no-op；之後任何新的攻擊性狀態只要動畫軌道會呼叫 `strike_impulse`，記得回來這裡加）。軌道路徑跟 `.:can_combo` 同一套慣例（NodePath 指向場景根節點 `.`，方法名 `strike_impulse`），時間點與力道在編輯器動畫面板調，不寫程式碼。配套修正：`VsAttack.enter()` 現在會先把 `velocity.x` 歸零（跑步衝進攻擊不再殘留慣性滑行），前衝感全部改由這個函式的動畫軌道呼叫負責。攻擊狀態的摩擦衰減改用 `VsAttack.IMPULSE_FRICTION`（見上）而非一般移動摩擦力，否則衝力煞不住會飛超遠。
  **⚠ 實測抓到過的 rollback desync**：`sync_anim_to_state()`（每次 rollback 還原快照都會呼叫、重模擬結束後也會再呼叫一次）內部用 `seek(elapsed, true)` 把動畫時間跳到還原後的位置。`can_combo`/`hitbox monitoring` 是「值」軌道，seek 重複套用同一個值無害（冪等）；但 `strike_impulse` 是「呼叫方法」軌道，每次真的觸發都會改一次 `velocity`，不是冪等的——Godot 的 `seek(..., true)` 是否會回放沿途經過的 Call Method key 行為不夠明確保證不會，一旦真的回放了，就會拿「已經是正確模擬結果」的 velocity 再蓋一次錯的衝力，而且每次 rollback 都疊加一次，實測連續幾次 rollback 後兩端 `vsattack` 狀態下的 `position`/`dash_energy` 對不上。修法：`VsPlayer._resyncing_anim` 旗標，`sync_anim_to_state()` 呼叫期間設 true，`strike_impulse()` 開頭檢查此旗標為 true 就直接跳過——只允許「真的走過這一幀模擬」（`_do_rollback` 重模擬迴圈裡的 `advance()`，或正常單幀模擬）觸發衝力，純視覺 resync 觸發的一律擋掉。**這是唯一的 Call Method 軌道，之後若新增別的 Call Method 軌道（不是值軌道）都要套用同一個 `_resyncing_anim` 防呆，否則遲早會踩到同一個坑。**
- **輔助鎖敵** — `VsPlayer.face_towards_x(x)` / `face_opponent()` 是共用輔助（`opponent` 由 vs_world 注入；用 `position` 算，確定性安全；rollback 走 `set_state_quiet` 不經過這些函式，面向由快照還原）。`VsIdle` 靜止（`input.move_dir == 0`）時**每幀**持續呼叫 `face_opponent()`，不是只在 `enter()` 算一次——對手左右移動時待機角色會跟著轉；一旦有移動輸入交回 `_apply_ground_move` 決定面向，不會互相蓋掉。**`enter()` 裡刻意不轉向**：`enter()` 跟上一個狀態的收尾轉場發生在同一模擬幀，這時 MANUAL 模式的 `play("idle")` 要等下一幀 `advance()` 才會真的把畫面換成 idle 姿勢，但 `graphics.scale.x` 是每幀無條件套用——若 `enter()` 就轉向，上一個狀態最後一幀顯示的畫面會被新朝向鏡像，看起來像「攻擊最後一幀也跟著轉」；轉向必須延到真正進入 idle（下一幀）由 `physics_update` 生效。

**MANUAL 動畫模式的轉場時機——兩種轉場的視覺延遲不一樣，別搞混**：`VsPlayer.apply_input()` 的呼叫順序是 `_apply_pending_hit()` → `anim_player.advance(delta)` → `state_machine.physics_update(delta, input)`。這決定了兩類轉場的視覺同步性完全不同：
  - **命中觸發的轉場**（`_apply_pending_hit()` 內直接呼叫 `state_machine.transition_to(...)`，例如受擊進 `vshurt`/`vslaunched`）發生在本幀 `advance()` **之前**——新動畫的 `play()` 呼叫完，緊接著同一幀的 `advance()` 就會推進新動畫，視覺當幀就會同步，沒有延遲。
  - **狀態自己 `physics_update()` 回傳字串觸發的轉場**（例如 `VsHurt`/`VsLaunched` 判斷條件成立後 `return &"vsknockdown"`，或任何「狀態跑完自己邏輯才決定換下一狀態」的轉場）發生在本幀 `advance()` **之後**——新動畫的 `play()` 要等到下一幀的 `advance()` 才會真的推進，本幀畫面還停在舊動畫最後一次 `advance()` 的姿勢。
  
  任何在 `enter()` 裡對「每幀無條件套用」的即時效果（`graphics.scale.x` 朝向、`position` 瞬移之類）做修改，只要是走**第二種**轉場，就會出現「邏輯已經生效、畫面卻還停在舊姿勢」的錯位（VsIdle 的鎖敵轉向、VsKnockdown 的瞬移都踩過這個坑）。修法是把該效果從 `enter()` 延後到該狀態自己 `physics_update()` 第一次真正執行時做（用一個旗標擋住只執行一次，記得存進快照）——那一幀的動畫視覺才真的追上。`VsPlayer._on_hurtbox_hurt()` 在 `is_invincible()` 檢查之後、不論後續走哪個分支（硬直/防禦/擊飛），立刻呼叫 `face_towards_x(hitbox.owner_player.position.x)` 面向攻擊者。`VsDodge.enter()` 算出 `dodge_dir` 後必須同時寫回 `facing_dir = dodge_dir`——不寫會出現「滑動動畫朝左、卻往右衝」（衝刺前後轉身時特別明顯）。
- **恢復狀態的跑步預輸入** — `VsPlayerState._recovery_transition(input)` 是共用收尾：地面+有方向輸入→直接 `vsrun`（同時把 `facing_dir` 設對，跳過「無腦轉 idle」再等下一幀才轉 run 的空檔）；地面無輸入→`vsidle`；未落地→`vsfall`。套用在 `VsDodge`/`VsHurt`（非落地分支）/`VsGetup`/`VsGuard`/`VsFall` 的收尾。**`VsAttack` 刻意不用**——攻擊收招不支援預輸入直接接 run，規則明講。
- **硬直/落地（倒地）/起身流程** — `causes_knockdown` 攻擊按擊退 y 分流（`VsPlayer._apply_pending_hit`）：y<0 → `VsLaunched` 擊飛（忽略硬直，弧線飛行，落地瞬間進 `VsKnockdown`、水平動量保留；空中再被擊飛=juggle 重入）；y=0 → `VsHurt` 正常硬直，結束後進 `VsKnockdown`（`queued_knockdown`）。`launched` 是純躺地姿勢，站姿切過去沒有任何過渡動作——解法是補一段一次性過場動畫 `launched_start`（站姿倒下→躺地），`VsKnockdown.enter()` 先播它，`physics_update()` 每幀檢查 `elapsed >= _start_anim_length` 播完就接 `LOOP_ANIM`（`launched`）循環；純視覺銜接，不影響下面的貼地/彈起/起身物理判定時機。（之前試過「進場把 y 瞬移到上方、重力帶回原位」的假摔方案，使用者否決了，改用這個補動畫的做法。）**「真正到地上」判定綁在 `launched_start` 過場動畫播完（`not _in_start_anim`），不是單純 `_grounded()`**——地面硬直（y=0 擊退）進來的角色從沒離地過，`enter()` 第一個 tick 就已經貼地，若拿 `_grounded()` 當判定，等於進場那一刻立刻無敵+彈起，前面「剛落地、還沒無敵」的窗口長度會變成 0（實測過的 bug：診斷數值顯示彈跳物理完全正確，問題其實是這段窗口被吃掉，玩家看到的第一幀就已經是彈起後下墜+無敵）。播 `launched_start` 期間＝「落地期間」，沒有無敵、也沒有任何命中限制；播完才觸發無敵直到起身（`INVINCIBLE_COVER` 罩住）。**2026-07-27 新增 `launched_3`（躺平動畫）**，插在「真正到地上」跟「彈起」之間——`launched_start` 播完立刻換播 `launched_3`（無敵從這一刻開始），停留 `launched_3` 自己的動畫長度，播完才真的彈起一次（`BOUNCE_VELOCITY = -160`，換回 `launched` 循環播放）→二次落地直接接 `VsGetup`（播 `launched_2`）。兩段窗口長度分別＝`launched_start`/`launched_3` 各自的動畫長度，在編輯器調動畫即可調整，不是額外的數字常數；角色動畫庫（`ClottyAnimLib.tres`/`NaiheAnimLib.tres`）要有一支叫 `launched_3` 的動畫，沒有的話 `get_animation()` 會回傳 null 報錯。此判定對地面硬直/空中擊飛落地兩條路徑都適用。貼地摩擦隨貼地時間漸增（`FRICTION_RAMP_TIME`），不瞬間黏住；倒地/擊飛中被普通攻擊打到只吃傷害與速度、不換狀態，沒有額外的命中限制保護這段期間（VsMods 沒有 OTG/juggle 限制欄位，見下）。`VsHurt` 硬直**不給任何無敵**（連段要能全程命中；同攻擊窗防重複靠 `has_hit`——別把 0.15s/0.2s 的「防穿透/喘息」無敵加回來，A2 打空的 bug 就是它）。`VsGetup` 播 `launched_2`、時長讀動畫長度。2026-07-27 使用者要求拿掉「起身後」額外的 2s 無敵，但起身動畫播放期間（無法操作的起身姿勢）仍要無敵——兩者不是同一件事，第一版誤把兩個都拿掉，導致起身動畫播放中也能被打，修正成 `invincible_time_left` 精確設成起身動畫長度（`_duration`），動畫播完那一刻自然歸零、不延續到操作權交還之後。⚠ `enter()` 一定要顯式設這個值，不能單純不寫——`VsKnockdown.INVINCIBLE_COVER`（10s）是罩住整段彈起流程的暫定大值，設計上本來就預期進 `VsGetup` 時會被校正掉，沒校正就會直接繼承那個 10s 一路撐到起身之後，變成「一直無敵」（第一輪修復時實測抓到的 bug）。衝刺打斷起身時無敵也跟著提前結束（`invincible_time_left = 0.0`）——提前結束就不再是無防禦的起身姿勢了。死亡演出：`VsRoundManager._tick_fighting` 判定回合結束時強制死者進 `VsKnockdown`（暫代死亡動畫）。AnimationPlayer 的 MANUAL 模式由 `VsPlayer._ready()` 在運行時設定，**不寫在 tscn**——否則編輯器動畫面板無法預覽播放。
- **空中普攻** — `VsAirAttack.gd`，獨立狀態（不是在 VsAttack 裡加分支），3 段連段，動畫時間軸/hitbox 數值/spawn_anim_vfx 呼叫從主遊戲 `Katana.gd` 的 `DICT_LIGHT_AIR`＋`player.tscn` 的 `air_light_1~3` 動畫逐一移植（素材 `player/Katana/air_A.png`，是一張 5×4 的 spritesheet，三段共用同一張圖、切不同 frame 區段，不是三張獨立圖）。判定框 `HitboxAir1~3`（跟地面 `HitboxA1~5` 是同一個 `VsPlayer.hitboxes` 集合，`_ready()` 通用收集）。從 `VsJump`/`VsFall` 的 `input.attack` 進入（且 `not VsPlayer.air_attack_used`，見下）；落地當幀立刻收招（空中連段不能延續到地面，`_grounded()` 一旦為真直接走 `_recovery_transition`）；判定框大小/位置是我抓的合理預設值（主遊戲三段共用同一顆 hitbox 沒有分開的數值可抄），建議進編輯器微調。**每次跳躍限用一輪**：`VsPlayer.air_attack_used`（落地時歸零，比照主遊戲 `air_attack_locked`），`VsAirAttack.enter()` 開始新一輪時設 true，`VsJump`/`VsFall` 的 `input.attack` 入口檢查此旗標——目前沒有移植主遊戲「衝刺可刷新這次機會」的 `refresh_air_skill_on_dash()`。

⚠ **重力/滯空手感（2026-07-18 最終定案，經過三次來回）**：主遊戲 `combo_step==13`（第 3 段）原本是「近零重力滯空」（`GRAVITY * 0.1 * delta`），中間一度改成 3 段全部滯空、又改回精確 1:1 port（只有第 3 段滯空），**但使用者最後實測覺得滯空手感本身就不好、比較喜歡第 1/2 段一般重力的感覺，所以現在 3 段全部統一用一般重力（`_apply_gravity(delta)`），完全拿掉滯空機制，這是刻意跟主遊戲不同的最終設計**，不要再擅自加回滯空。為了補償拿掉滯空後第 3 段掉更快，**第 3 段的起跳推力加大**：`AIR_THRUST_FORCE_FINISHER = -300.0`（VsMods 原創數值，主遊戲沒有這個區分），只有 `combo_step==3` 的 `enter()` 用這個值，1/2 段照舊用 `AIR_THRUST_FORCE = -150.0`。**上升推力**：主遊戲 `_play_air_step()` 在**每一段**觸發當下都直接 `player.velocity.y = air_thrust_force`（-150.0，往上）——不是只有第一段，連段每一下都會把角色往上頂一點，這部分原樣照搬（一開始漏做，後來補上），第 3 段則是在此基礎上再加大成 -300。
- **技能（`input.skill`）** — `VsSkill.gd`，單發（不是連段）、地面限定（規則沒說能在空中放，比照防禦的保守預設）。定義特徵是**強破霸**：`HitboxSkill` 節點的 `break_level` 直接設 `STRONG_ARMOR_BREAK`。從 `VsIdle`/`VsRun` 的 `input.skill` 進入。⚠ **骨架先行，動畫是佔位**：`skill` 動畫目前借用 idle 的單格圖頂著（`Katana Skill.png` 是一張 6 排、每排十幾格的複合動作圖，切哪一段給使用者自己決定，我沒有亂猜），使用者會自己把 texture/hframes/vframes/region_rect/frame 軌換成 `Katana Skill.png` 對應的段落；`HitboxSkill:monitoring` 開關窗、`_anim_length` 都是照動畫軌道自動讀，換動畫後不用改 `VsSkill.gd`。
- **武藝（`input.art_1/2/3`）** — 框架已通、只有一招範例效果。`VsMartialArt.gd`（`extends VsPlayerState`）是所有武藝的共用基底，`@export art_id/art_name/energy_cost/can_use_in_air/armor_tier_override_strong`；`armor_tier_override_strong` 直接接進 `VsPlayer.get_armor_tier()`（`cur is VsMartialArt` 分支依此欄位回傳 `STRONG_HYPER` 或 `HYPER`，武藝全程霸體以上的規則就是靠這個欄位落地，不是另開一套判定）。**武藝狀態不是場景固定子節點**——`VsAttack`/`VsSkill`/`VsAirAttack` 是 tscn 裡的靜態子節點，但武藝要等 `VsGameManager.p1_arts/p2_arts` 選好之後才知道該裝哪支腳本，所以走動態註冊：`VsPlayer._load_arts()`（`vs_world._spawn_players()` 在 `apply_arts_bonus()` 之後呼叫）逐槽用 `VsArtRegistry.get_art_script(art_name)` 查腳本路徑、`new()` 一個節點、`node.name = "VsArt%d"`、掛進 `state_machine` 底下，再呼叫新增的 `VsStateMachine.register_state(state, player)`（把 `player`/`state_machine` 反向引用補上、塞進 `states` 字典，鍵是小寫節點名——即 `"vsart1"/"vsart2"/"vsart3"`，跟原本 `_ready()` 時期的自動探索走同一把字典，之後 `transition_to("vsart1")` 完全不用另外處理）。`VsArtRegistry`（純靜態字典 `art_name → 腳本路徑`）是因為 `VsGameManager` 只存武藝名稱字串（不像主遊戲 `equipped_martial_arts` 存完整 .tscn 路徑），效果實作時要在這裡登記。`VsPlayerState._check_art_cast(input)` 是共用打斷判定（`VsIdle`/`VsRun`/`VsJump`/`VsFall`/`VsAttack`/`VsAirAttack`/`VsSkill` 都在各自打斷優先級中呼叫）：解析 `input.art_1/2/3` → 查 `VsPlayer.get_art_in_slot(slot)` → 若非空中招式擋在空中施放、已在同一招內、或能量不足（`use_arts_energy` 內建判定+扣血）就回傳 `&""` 表示不可用，否則回傳目標狀態名。**武藝是角色綁定的**：`VsGameManager.CLOTTY_ARTS` 的六招（`Art_Clotty_1~6`）對應主遊戲 `player/MartialArts/Katana/Art_Katana_1~6.gd` 六支既有招式（Clotty 綁定 Katana），不是憑空設計新效果——移植方式是「動畫複製過來、邏輯改成 VsMods 的確定性寫法」，先去讀主遊戲對應腳本的實際效果再照搬，不要自己編。目前完成度：
  **六招全部完成**（`VsArtRegistry` 已全數登記）：
  - `Art_Clotty_2.gd`（升龍，地面版）：完整移植主遊戲 `Art_Katana_2.gd` 的自我彈射機制——起手 `LAUNCH_START_TIME`(0.1s) 後、`LAUNCH_DURATION`(0.1s) 窗口內把自身速度鎖定成 `VERTICAL_LAUNCH_SPEED`(-650)/`FORWARD_LAUNCH_SPEED`(380)，窗口結束後上升尾段用 2 倍重力煞停、下落尾段一般重力，數值原樣照搬。單發判定（主遊戲原版是 4 連段 sticky，使用者說「也是普通的攻擊」，簡化成單發）。`HitboxArt2` 的 `knockback.y<0` + `causes_knockdown=true` 讓中招對手直接進 `VsLaunched`，跟升龍的定位天然吻合。動畫完整移植自主遊戲 `Art_Katana_2` 動畫資源（texture `player/Katana/C1.png`）。
    - **同一武藝槽在空中施放時打的是完全不同的另一招「降龍」**——比照主遊戲 `Katana.gd` 同一個 combo_step 系統裡的「三段式下墜戰技」（`combo_step 25` 下墜前準備 → `26` 下墜循環 → `27` 落地，即 `air_heavy_*` 系列，不是升龍的空戰版，只是共用同一個武藝槽施放）。`Art_Clotty_2.enter()` 當下依 `_grounded()` 決定走地面（升龍）或空中（降龍）分支，`is_air_mode`/`air_stage`（`AirStage.START/LOOP/LAND`）都存進快照。三段物理：`START`（`art_2_air_start`，`AIR_ENTRY_THRUST=-300` 起跳、`IMPULSE_FRICTION*0.2` 摩擦、`GRAVITY*0.5` 半重力，動畫播完進 LOOP）→ `LOOP`（`art_2_air_loop`，循環動畫，`IMPULSE_FRICTION*0.5` 摩擦、`velocity.y` **直接鎖定**成 `AIR_DIVE_SPEED=800`（不是加速度，原樣照搬主遊戲 `new_y = 800.0`），`HitboxArt2Air`（5 連擊/0.1s 間隔/sticky，數值照搬 `combo_step==26`）用程式碼直接開啟 `monitoring=true`（無動畫關閉 key，靠落地轉場關閉，不違反 tracks-over-code——這裡的關閉條件本來就是物理事件不是動畫時間點），`_grounded()` 一到就進 LAND）→ `LAND`（`art_2_air_land`，`velocity` 清零、`vfx_shake(25.0)` 落地震動照搬主遊戲 `apply_camera_shake(25.0)`，動畫播完 `_recovery_transition`）。三段動畫共用同一張 `air_A.png`（`41_tx_air_a`，跟空中普攻同一張圖，不用新增材質），frame 索引跟空中普攻第 3 段（`combo_step==13`）共用同一段（`8,12~18`）。`HitboxArt2`（地面版）/`HitboxArt2Air`（空中版）各自獨立，`enter()`/`exit()` 都要同時處理兩顆（`enter()` 硬重置用 `_reset_hitbox`/`_reset_air_hitbox`，`exit()` 走 `close_on_state_exit()` 尊重 sticky，兩套語意不能混用）。⚠ **實測抓到的 bug**：`_enter_air_land()`（LOOP→LAND 的階段轉場，不是整個狀態 `exit()`）一開始也錯用了硬重置 `_reset_air_hitbox()`，導致落地那一刻不管 sticky 連擊打到第幾下都被強制砍斷（使用者實測：開了 sticky，一落地傷害還是立刻停）——階段轉場（LOOP→LAND）**不是**離開整個狀態，sticky 連擊沒打完該讓它繼續跑，所以改成跟 `exit()` 同一套 `close_on_state_exit()`（尊重 sticky，只有沒開或已經打完才真的關）。之後任何「同一個狀態內部換階段」的轉場都要用這個判斷方式，只有「真正離開整個武藝/狀態」才算數地用 `_reset_*` 硬重置。
  - `Art_Clotty_3.gd`：單發、可在空中施放（`can_use_in_air=true`），空中時套用 0.25 倍重力（原樣照搬主遊戲 `air_skill_gravity_rate`）。主遊戲原版還有「破防+世界時緩」演出，VsMods 明確禁用 `time_scale` 時停，使用者也說這招「沒有其他特點，只要動畫過來就好」，直接跳過，當一般攻擊處理。動畫完整移植（texture `player/Katana/C2W.png`），額外加了一個使用者要求的原版沒有的「cut」揮砍特效——⚠ 這裡曾經漏看過主遊戲該動畫實際上總共 12 條軌道（不是我最初讀到的 10 條），第 11 條才是真正的 4 個 `spawn_anim_vfx`（"Aggregation ring" + 3 個不同色調的 "cut"），後來補上這 4 個真實資料取代掉我自己瞎加的那個，之後讀取主遊戲動畫資料務必讀到資源區塊真正結束（下一個 `[sub_resource`/`[node`），不要憑經驗猜測track數量提早停。
  - `Art_Clotty_5.gd`（斷空劍氣）：主遊戲原版可長按蓄力發射 1~6 發劍氣，使用者要求砍掉蓄力機制，簡化成「出招→發射一發劍氣→收招」。發射由脫手彈道 `VsProjectile`（見下）負責，時機交給動畫 Call Method 軌道（`fire_sword_wave`），不寫死在程式碼。動畫完整移植（texture `player/Katana/C3.png`）。
  - `Art_Clotty_1.gd`（逆鱗返/彈反）：原地格擋預備 `CHANNEL_DURATION`(2.0s)，期間（`Stage.START` 前搖 + `Stage.LOOP` 循環都算判定區間）被攻擊命中就完全格擋、射出劍氣反擊（`fire_sword_wave(700.0)`，700 傷害走 `damage_override` 參數覆寫，見下）、獲得 `COUNTER_INVINCIBLE_DURATION`(1.0s) 無敵；沒被打中則 `channel_timer` 歸零後進 `Stage.END` 自然收招。**跟主遊戲唯一不同的規則調整**（使用者明確要求）：主遊戲原版完全格擋任何攻擊，這裡改成只格擋「強破霸以下」的攻擊（`hitbox.break_level < STRONG_ARMOR_BREAK`），銜接 VsMods 既有的體質系統；格擋成功後的效果跟原版數值一樣。實作介面：`try_parry(hitbox: VsHitbox) -> bool`，由 `VsPlayer._on_hurtbox_hurt()` 在 `is_invincible()` 早退之後、`is VsGuard` 分支之前 duck-typed 呼叫（`cur.has_method("try_parry") and cur.try_parry(hitbox)`），回傳 true 代表這下攻擊已被完全吃掉，不進入後續傷害/硬直流程——之後任何想做類似「條件性完全格擋」的武藝都能照這個介面接，不用改 `VsPlayer.gd`。4 個動畫全部移植自主遊戲（`art_1_start`/`art_1_loop`/`art_1_end`/`art_1_counter`，texture 分別是 `Art_Katana_1.png`（192×192, 3×3）跟反擊動畫專用的 `A2.png`（384×192, 3×3））。**這招沒有自己的攻擊 hitbox**（舊版單發突刺範例的 `HitboxArt1`/`Shape_hbArt1` 已整個移除）——它的攻擊手段是格擋後的劍氣反擊，不是近戰判定框。
  - `Art_Clotty_4.gd`/`Art_Clotty_6.gd`：來源不是 `Art_Katana_4/6.gd`，而是主遊戲武器**重斬連段**（`Katana.gd` 的 `DICT_HEAVY_ULT`）——使用者這次明確指定直接抓武器重攻擊的動畫/數值當武藝範本。`Art_Clotty_6` = `combo_step 21`（"katana/heavy_1"，texture `C2.png`，`combo_hits=3/combo_interval=0.1/sticky=true`，`damage=200/knockback=(100,0)`）；`Art_Clotty_4` = `combo_step 22`（"katana/heavy_2"，texture `A5.png`，`combo_hits=8/combo_interval=0.05/sticky=true`，`damage=200/knockback=(50,-50)`）——兩招數值都是 `max_hits`/`interval`/`sticky:true` 原樣照搬進新的連擊系統（見下）。都沒有其他特點，跟 `Art_Clotty_3` 同一套簡單模板（`enter()` 播動畫、`IMPULSE_FRICTION` 摩擦力煞停、動畫結束 `_recovery_transition`）。
  - `VsPlayer.fire_sword_wave(damage_override: float = -1.0)`：新增選填參數，`> 0` 時覆寫這顆彈道的傷害（`Art_Clotty_1` 反擊劍氣用 700，跟 `Art_Clotty_5` 一般劍氣的預設值 120 不同），`<= 0`（預設）就沿用 `VsProjectile.tscn` 節點上的值，呼叫端不用每次都指定。
  - ⚠ **每招套用 `strike_impulse` 摩擦力時，務必回去核對主遊戲原始碼裡該 `combo_step` 實際用的倍率，不能都當滿檔 `IMPULSE_FRICTION` 處理**——這幾次移植踩過兩次同類 bug：`Art_Clotty_1` 反擊動畫的 `strike_impulse(-1200)` 一開始漏套 `IMPULSE_FRICTION`（用了一般移動摩擦力 `FRICTION`=900，飛太遠）；`Art_Clotty_6`（heavy_1／combo_step 21）主遊戲 `Katana.gd::get_current_velocity()` 對這個 combo_step 特別用 `base_friction * skill_neutral_friction_rate`（`skill_neutral_friction_rate` 預設 **0.2**，只有一般攻擊摩擦力的 20%——這招設計上打出去會拖著滑一段距離，不是打完立刻煞停），跟 `Art_Clotty_4`（heavy_2／combo_step 22，滿檔 `base_friction`）不一樣，一開始兩招都套了滿檔導致位移軌跡跟主遊戲對不上。`Art_Clotty_6.gd` 現在用 `IMPULSE_FRICTION * FRICTION_RATE(0.2) * delta`。
  - `Art_Clotty_4` 擴充（使用者要求，VsMods 原創、主遊戲沒有）：`can_use_in_air = true`，x 摩擦力不分地面空中統一用 `IMPULSE_FRICTION`。**重力手感來回調了四次才定案**：0.1 倍滯空（剛跳起會被拖飛很高）→只在下墜階段滯空→跟 1/2 段一樣一般重力+加大起跳推力補償→使用者實測都不滿意，**最終直接要求完全無視重力**：`velocity.y` 施放全程鎖定 `0.0`（`enter()` 空中施放時先設一次，`physics_update()` 每幀再蓋一次，雙重保險），原地懸空不掉不升，不套用任何重力係數。因為完全不會掉落，原本補償用的起跳推力常數已經沒意義，直接拿掉。之後如果還有類似「空中招式滯空手感」的需求，直接問清楚是要哪一種：有重力but減速（近零重力）、無重力但有初速（純慣性飄移）、還是完全鎖定不動（這次的最終選擇）——這三種手感差異很大，不要用同一套邏輯硬套。
  - `Art_Clotty_1` 修過的 bug：`physics_update()` 一開始完全沒接衝刺取消檢查，導致格擋/反擊期間衝刺打不出來，違反「衝刺可打斷任何非受擊動作」的全域規則（見下方 `is_input_locked` 一節——dodge 的例外要在每個輸入處理點手動接上，不會自動繼承，新增任何狀態都要記得補這行）。已在 `physics_update()` 最前面補上跟其他所有狀態一致的 `if input.dodge and vs.use_dash_energy(30.0): return &"vsdodge"`。

**脫手彈道（`VsMods/combat/VsProjectile.gd` + `.tscn`）**——武藝系統第一次需要「離開施放者、自己飛行判定」的攻擊，是本專案的 `VsHitbox.detached` 概念第一次真正落地（此前只有欄位宣告，沒有配套機制）：
  - `VsProjectile extends Node2D`，必須以 `vs_world` 的直屬子節點形式存在（`position` 才等同世界座標，同一套 `_sim_rect` 的確定性慣例）。`simulate(delta)` 是無狀態的直線位移累加，由 `vs_world._update_projectiles()` 每幀手動呼叫推進，不用 `_physics_process`/`move_and_slide`。
  - **生成入口**：`VsPlayer.fire_sword_wave()`，動畫 Call Method 軌道呼叫（跟 `strike_impulse` 同一套慣例）。只擋 `_resyncing_anim`（純視覺 seek resync 誤觸發），**不擋 `is_resimulating`**——這裡是「生成彈道」這個模擬狀態的一部分，rollback 重模擬同一幀時必須照樣真的生成新節點，兩端結果才會一致；跟 `vfx_*` 系列（純視覺、必須被 `is_resimulating` 擋掉重複觸發）性質相反，別搞混。
  - **命中判定**：`vs_world._manual_check_projectile()` 比照 `_manual_check()` 的手動幾何 `Rect2` 相交，複用 `VsHitbox` 既有的 `has_hit`/`hit_targets` 防重複機制；位置基準另外寫 `_sim_rect_projectile()`（用彈道自己的 `position` + `direction` 決定鏡像，不是 `_sim_rect()` 假設的「相對某個 VsPlayer.facing_dir」）。
  - **Rollback 快照**：`vs_world.projectiles: Array[VsProjectile]` 整包存進 `_save_snapshot`（`"proj"` key，含 owner 的 `player_id`），還原時 `_restore_projectiles()` 直接砍掉現有節點重新生成再 `restore_state()`——彈道數量少，用「整批重建」換取正確性，不用逐一比對增刪。這個設計成立的前提是：只要 restore 到「彈道尚未生成」的那個快照點，重模擬會重新跑到 `fire_sword_wave()` 那一幀、自然重新生成同一顆彈道；只要 restore 到「彈道已存在」之後的快照點，`"proj"` 資料本身就已經含有它，不會漏。
  - Checksum：彈道數量與座標併進 `_compute_checksums()` 的位置雜湊欄位，數量或位置分歧（例如某一端漏生成/漏移動一顆）會被 desync 偵測抓到，不是獨立欄位。
  - 視覺沿用主遊戲 `c_3_wave.tscn` 的貼圖 `Explod/Sprite-0004.png`（靜態單幀，沒有搬主遊戲的 fade_out 動畫演出，銷毀直接 `free()`，不靠 AnimationPlayer 的 `queue_free` Call Method key——少一個要顧 rollback 確定性的活動部件）。
  - 已知限制：沒有處理「回合切換時殘留彈道」的清理（新回合開始若還有彈道飛在空中不會被強制清掉，靠自然的 `max_distance`/命中收尾），這是刻意先不做的邊界情況，之後要做的話必須讓清理邏輯本身也是確定性模擬的一部分，不能用 `is_resimulating` 擋掉（會破壞 rollback 重跑時的一致性）。

**連擊（多段命中）與 `sticky`（黏著連段，命中後不再檢查幾何重疊）**——`VsHitbox` 新增 `@export_group("連擊")`：`combo_hits: int = 1`（這顆判定框最多可命中幾次，預設 1 = 一般單發，完全維持舊行為）、`combo_interval: float = 0.0`（每次命中之間的最短間隔，第一下不受限）。運行期欄位 `hits_dealt`/`hit_cooldown_left` 取代了原本單純的 `has_hit` 布林——`has_hit` 現在是 `hits_dealt >= combo_hits` 的衍生結果，由 `VsHitbox.register_hit()` 統一維護（遞增次數、套用間隔、次數用完才標記 `has_hit`），不要在別處直接寫 `has_hit`。`vs_world._manual_check()`/`_manual_check_projectile()` 每幀開頭先呼叫 `hb.tick_cooldown(delta)`（吃模擬 delta，不是真實時間），命中判定門檻從單純 `has_hit` 改成 `has_hit or hit_cooldown_left > 0.0`；命中成功一律呼叫 `hb.register_hit()`，`has_hit` 一旦變 true 就立刻 `monitoring = false`（連擊全部打完才關）。`1v1 只有一顆對手 hurtbox`，`hit_targets` 字典因此不再當命中門檻用（連擊需要重複命中同一目標，跟舊的「每個目標只能被同一顆 hitbox 打一次」語意衝突），純保留給未來多目標情境／除錯。

`sticky: bool`（比照主遊戲 `Hitbox.sticky_multi_hit`）：**true = 第一下命中後鎖定目標（`is_locked=true`），後續連擊不再檢查幾何是否還重疊，冷卻時間一到就自動觸發傷害，直到連擊數用完**。`vs_world._manual_check()`/`_manual_check_projectile()` 的命中判定改成 `(hb.sticky and hb.is_locked) or 幾何相交`——鎖定前走一般的幾何檢查（這也是連擊第一下的必經路徑），鎖定後直接跳過。1v1 只有一個對手，「鎖定」不需要存目標節點引用，`is_locked` 一個 bool 就夠（不用煩惱 rollback 快照要存哪個節點）。⚠ **這個設計取代了先前的 `detached` 欄位**（原本只處理「狀態結束後判定框是否還存活」，沒解決真正的問題：像升龍這類攻擊者自己會被彈飛的連段，中間幾下攻擊者早就跟目標分開了，若還要求幾何重疊，後續傷害幾乎打不到）——`sticky` 直接讓已鎖定的連擊不再依賴幾何重疊，天然也就不受施放狀態存活時間限制，不需要 `detached` 這個獨立開關。實作上，每個擁有 hitbox 的狀態（`VsAttack`/`VsAirAttack`/`VsSkill`/`Art_Clotty_1~3`）的 `exit()` 都從直接寫 `monitoring=false; reset_hits()` 改成呼叫 `VsHitbox.close_on_state_exit()`（`sticky and not has_hit` → 跳過；否則照舊強制關閉+重置）——**`enter()` 的硬重置（`_reset_hitbox(es)`，包含連段內部 `enter()` 重啟）維持不變、不受 `sticky` 影響**，只有「離開整個狀態」這個時間點才尊重它。跟 `VsProjectile`（真正脫離角色飛行的彈道）是不同機制：這裡的 hitbox 仍是角色身上的節點、位置仍跟著角色，只是「判定不再需要幾何重疊」。`VsPlayer.save_state()`/`restore_state()` 的 `"hbs"` 欄位已擴充成 `[monitoring, has_hit, hits_dealt, hit_cooldown_left, is_locked]` 存進快照，連擊進度（含鎖定狀態）rollback 安全。

⚠ **實測抓到的連擊 bug**：`combo_hits` 設 5/10，結果都只打出 4 下；改用 `HitboxA1`（一般地面普攻的判定框）測 2 連擊，結果連第二下都沒有。根因是同一個：判定框的開關時間點是動畫軌道照「單發攻擊」設計的，關閉那個 key 完全不知道 `combo_hits` 這回事，時間一到就無條件把 `monitoring` 設回 `false`，導致連擊還沒打完視窗就被動畫自己關掉（跟 `sticky` 有沒有開無關——`sticky` 只解決「還要不要幾何重疊」，不解決「視窗被動畫關掉」）。修法：`vs_world._manual_check()`/`_manual_check_projectile()` 在 `tick_cooldown()` 之後、`monitoring` 早退判斷之前，加一段「連擊已經開始但還沒打完（`combo_hits > 1 and hits_dealt > 0 and not has_hit`）就強制 `monitoring = true`，無視動畫這一幀剛設的值」——連擊一旦開打，只由 `register_hit()` 的 `has_hit` 或狀態 `exit()` 的 `close_on_state_exit()` 決定何時真正關閉，動畫軌道的關閉 key 只負責「這招從沒打中過」時的自然收尾。

⚠ **`can_hit_downed`/`can_hit_launched` 這整個機制已經拿掉（2026-07-18，使用者最終決定）**：VsMods 曾經短暫做過一個「這顆判定框能不能打中擊飛中（`VsLaunched`）目標」的欄位（一開始誤植成檢查 `VsKnockdown`，除錯後發現使用者的真實設計意圖是 `VsLaunched`，比照 MUGEN 的 `S`/`C`/`A` 攻擊分類），配套還做了「啟動快照」防呆（`VsHitbox._was_monitoring`/`blocked_by_launched_snapshot`，防止 `causes_knockdown` 自己造成的狀態變化鑽漏洞）。使用者實際用過後覺得「每招預設打不到擊飛目標」造成連段很難用，權衡過「預設改成可以打、少數招式手動關」的折衷方案後，最終決定**整個刪掉**，不留任何命中限制欄位——現在任何攻擊都能命中任何狀態的目標（`VsKnockdown`/`VsLaunched` 皆然），倒地/擊飛流程的保護單純靠自己的無敵/彈起時序（見上），不受攻擊屬性影響。如果之後又想做類似限制，記得問清楚是要「打倒地的人」的 OTG 概念、還是「打擊飛/追打空中對手」的 juggle 限制——這兩者當初被搞混、一路除錯又重新定義又整個拿掉，是這幾輪反覆的根本原因，別重蹈覆轍。

⚠ **除錯方法論**：這次抓漏洞的過程是先在 `vs_world._manual_check()` 的關鍵分支加 `print()`（判定框開窗當下的快照結果、每次真正命中時的完整狀態），請使用者重現一次、貼 Godot 輸出面板的訊息回來，直接比對「目標當時到底是什麼狀態」——比純推理有效很多，兩次都是這樣才分別揪出「causes_knockdown 一直重新打飛」和「使用者對倒地/擊飛的定義從一開始就跟我以為的不同」這兩層問題。之後遇到類似「邏輯看起來對、但實測不對」的情況，優先加 print 讓使用者重現再看 log，不要一直憑推理猜測。這個方法論後續在 Phase 9 的擊退方向 bug 上又驗證了一次（見下）。

**多段連擊的擊退方向鎖定**（Phase 9，補充上面 sticky 段落）——`VsHitbox.direction_override`（0=未覆寫，非 0=鎖定方向）：`VsPlayer._on_hurtbox_hurt()` 在 `hitbox.combo_hits > 1 and hitbox.hits_dealt == 1` 時把命中當下的 `dir_x` 存進去，之後連擊每一下優先讀這個值，不再重新讀 `owner_player.facing_dir`。根因：sticky 連擊允許存活超過施放者離開該狀態之後（見上），如果每一下命中都重新讀「攻擊者當下面向」，攻擊者事後自由移動轉身會讓殘留的連擊擊退方向跟著亂跳（實測案例：`HitboxArt2Air` 落地後角色已經在 `VsRun` 到處走，擊退方向跟著遊走）。`VsHitbox.reset_hits()` 會清空這個欄位，每次重新施放都要重新鎖。**第一版修法方向錯了**：曾經嘗試把 `close_on_state_exit()` 限縮成只給同招內部換階段用、所有狀態真正 `exit()` 一律硬重置——這樣是解決了方向亂跑，但也讓 sticky 連擊只要玩家離開招式就被提前砍斷，等於在兩個各自成立的需求之間選錯了要犧牲哪一個。正確做法是鎖定方向而不是限制存活時間，`close_on_state_exit()` 維持原本寬鬆語意不變。彈道類武藝（`Art_Clotty_1`/`5`）也有同款問題與同款修法：`VsProjectile` 生成/`restore_state()` 時就把方向鎖死，不會因為飛行途中施放者轉身而改變。

### VsMods 選角色系統（角色繼承拆分）

`VsMods/player/VsCharacterRegistry.gd` — 角色 id → `{display_name, scene_path, arts}` 靜態表（跟 `VsArtRegistry.gd` 同一套 pattern，`class_name` 不掛 autoload），`get_scene()`/`get_arts()`/`get_display_name()`/`all_ids()`。`vs_world._spawn_players()` 用這個查表各自生成兩個玩家的場景，不再共用同一份 `preload`。

角色資料透過 Godot Inherited Scene 拆分成框架/角色兩層：`VsMods/player/VsPlayerBase.tscn`（真正空白骨架——碰撞層慣例、13 個狀態子節點、9 顆通用判定框節點但無數值/shape、Sprite2D 無貼圖、AnimationPlayer 無 libraries）+ `VsMods/player/VsPlayerClotty.tscn`（Inherited Scene，繼承 base，覆寫根節點屬性、Sprite2D 貼圖、9 顆通用判定框完整數值+shape、動畫庫指到外部資源 `VsMods/player/anim/ClottyAnimLib.tres`，並**新增** Clotty 專屬的 5 顆武藝判定框 `HitboxArt2`/`HitboxArt2Air`/`HitboxArt3`/`HitboxArt4`/`HitboxArt6`——這些不在 base 裡）。`VsCharacterRegistry.CHARACTERS["Clotty"].scene_path` 指向 `VsPlayerClotty.tscn`；原始未拆分的 `VsMods/player/VsPlayer.tscn` 保留但未被使用，當作保險備份。

**Godot Inherited Scene 的文字格式其實就是普通場景檔**，不是特殊的 diff 語法——derived 場景只是 `[node name="X" parent="Y"]` 列出覆寫過的屬性（沒有 `type=`），只有真正新增的節點才需要 `type=`。可以直接文字編輯（確認編輯器完全關閉後），不需要靠編輯器「還原箭頭」清空 base 值再手動覆寫（那個不可靠，實測會漏清部分屬性、CollisionShape2D 的 shape 反而被誤清成 `null`）。

角色專屬的基礎數值全部走 `@export`，不是共用 const：`VsAttack.max_combo`/`VsAirAttack.max_combo`（連段段數，原本是 5/3 的寫死 const）、`VsPlayer` 的 `@export_group("基礎移動數值")`（`gravity`/`move_speed`/`air_speed`/`friction`/`jump_force`，原本是 `VsPlayerState.gd` 的共用基底 const；`VsDodge.DODGE_SPEED` 原本也是獨立 const）——各狀態腳本改讀 `(player as VsPlayer).xxx`。之後角色想要不同跑速/跳躍高度/連段數，直接在自己的 derived 場景改根節點屬性即可，不用碰程式碼。

**`Mech`/`Ripple`（2026-08-03，佔位角色）**——跟 Naihe/Asatsubaki 當初起步時同一套「先複製再說」：`VsPlayerMech.tscn`/`VsPlayerRipple.tscn` 都是 `VsPlayerClotty.tscn` 的原樣複製，各自帶獨立的 `MechAnimLib.tres`/`RippleAnimLib.tres`（複製自 `ClottyAnimLib.tres`，理由同上——共用同一份會讓其中一邊改動畫時把另一邊一起改壞）。**這兩個角色目前沒有任何美術/動畫素材**（`VsMods/ui/Mechl.png` 是舊版 VsMods 重建前的殘留檔案，只是個箭頭圖示，跟這次的 Mech 無關，未使用），Sprite2D 貼圖沿用 Clotty 的 `IDLE.png` 佔位；武藝沿用 Clotty 的六招（`Art_Clotty_1~6`）當佔位，招式來源使用者明確表示「不用管」（先不設計，之後有想法再說）。因為是純文字複製（不是透過編輯器另存新場景），兩份 `.tscn`/`.tres` 檔頂層的 `uid=` 屬性被移除（沿用原檔的 uid 會跟 Clotty 的檔案衝突）——Godot 下次在編輯器開啟時會自動補上新的 uid，不用手動處理；AnimLib 的 `ext_resource` 也改成純路徑引用（不帶 uid）。之後要換真正的美術/招式，直接改這兩個角色各自的 `.tscn`/`AnimLib.tres`/`VsCharacterRegistry.CHARACTERS["Mech"/"Ripple"].arts`，跟 Naihe/Asatsubaki 走過的路一樣，不用動框架程式碼。

### VsMods Asatsubaki 血影機制（技能逐角色可覆寫，2026-08-03）

**技能（VsSkill）從全角色共用的靜態節點改成可選的逐角色覆寫**——比照武藝的動態註冊模式，但預設零風險：`VsCharacterRegistry.CHARACTERS[id]` 新增可選欄位 `"skill_script"`（空字串/不填＝沿用 `VsPlayerBase.tscn` 內建的靜態 `VsSkill` 節點，Clotty/Naihe 完全不受影響）。`VsPlayer._load_skill()`（`vs_world._spawn_players()` 在 `_load_arts()` 之後呼叫，時機比照 `art_slots` 注入——`_ready()` 當下還不知道 `character_id`）讀表，有指定才動態換掉靜態節點：跟 `reload_arts()` 同一套「同名節點命名陷阱」處理，先 `state_machine.remove_child(old)` 讓名字立刻釋放、`old.queue_free()`，同一幀才 `add_child()` 一顆同名（`"VsSkill"`）新節點＋`register_state()`，否則 Godot 會自動改名成 `"VsSkill@2"`，`transition_to(&"vsskill")` 會找不到狀態。`VsPlayer.character_id`（新增欄位）是這個查表的依據，由 `vs_world._spawn_players()` 在 `add_child()` 之後設定。

Asatsubaki 的 `VsSkill_Asatsubaki.gd`（`VsMods/player/states/VsSkill_Asatsubaki.gd`，`extends VsPlayerState`，不繼承 `VsSkill.gd`——行為完全不同）——技能鍵召喚一個可操控的「血影」分身，可以把操控權轉移過去自由飛行偵查/牽制：

- **召喚**（`vs.shadow == null`）：扣固定血量（`take_damage(SUMMON_COST)`，目前 150）、播召喚動畫（骨架先借 idle 圖頂位，跟 `VsSkill.gd` 當初同一個「先求動起來，動畫使用者自己換」慣例）、呼叫 `vs_world.spawn_blood_shadow(vs)` 生成血影存進 `vs.shadow`，動畫播完照一般技能收尾。
- **轉移操控權**（`vs.shadow != null`）：`enter()` 不播動畫，直接 `vs.is_possessing = true` 後在尾端呼叫 `state_machine.transition_to(&"vsidle")`——巢狀轉場（跟 `VsAttack` 連段延續「呼叫 `enter()` 繞過防重入鎖」同一類手法），讓角色本體的 `current_state_name` 正確落在 vsidle 待機，不是卡在這個過場節點。此後角色的狀態機**不會再被 tick**：`VsPlayer.apply_input()` 開頭一旦 `is_possessing==true` 就直接呼叫 `_apply_possession_input()` 後 `return`，完全跳過 `_apply_pending_hit()`/`state_machine.physics_update()`/一般移動——角色凍結在 vsidle 的姿勢，能量回復/無敵倒數/buff 倒數這幾個純被動計時器（跟輸入無關）維持照跑。

**操控血影**（`VsPlayer._apply_possession_input()`）：讀 `input.move_dir`/`input.jump`，呼叫 `shadow.move(delta, move_dir, jump_pressed)`。長按技能鍵 1 秒銷毀血影：`InputState` 新增一個**持續按住**版欄位 `skill_held`（跟原本 edge-triggered 的 `skill` 同一顆鍵並存）。

`VsBloodShadow.gd` 現在是 `CharacterBody2D`（原本是純 `Node2D`，2026-08-03 改版）——**會被牆壁/地板擋住，跟角色本體一樣，不是幽靈**：`move()` 用跟 `VsPlayer._move_deterministic()` 同一套 `move_and_collide()` 確定性寫法（碰到東西依法線判斷該歸零哪一軸、剩餘位移補推一次，貼牆/貼地滑動不卡死），嚴禁 `move_and_slide()`（見確定性規則表）。`collision_layer=0`（不需要被任何人偵測到）、`collision_mask=4224`（128 地板層｜4096 平台層，跟 `VsPlayer.GROUND_LAYER_BIT`/`PLATFORM_LAYER_BIT` 同一組值），直接寫在 `VsBloodShadow.tscn` 根節點屬性。不做階梯輔助——沒人要求，撞到台階當一般牆壁擋住即可。

**吃重力＋可以跳躍**（同日再改版：原本是「無重力四方向自由飛行」，使用者實測後覺得飛行手感不對，要求直接改回一般平台動作邏輯，`up_held`/`is_crouch` 的上下自由移動整個拿掉）：垂直方向 `velocity.y` 累積 `SHADOW_GRAVITY`（850，獨立常數，**不是**跟本體共用——本體 980，使用者實測後明確要求血影要飄得比本體輕，兩者刻意不同；數值本身後來由使用者直接在腳本改過一次，見檔案內常數，之後調整以檔案為準）、落地時按跳躍鍵可跳，跳躍力道讀 `owner_player.effective_jump_force()`（本體之後調跳躍高度血影自動跟著變）；水平方向仍用血影自己的 `SHADOW_MOVE_SPEED`（350，原 220，使用者先前要求調快，維持獨立於本體速度）。`grounded` 用跟 `VsPlayer` 同款的 `test_move()` 純位置查詢（無隱藏狀態），不是 `CharacterBody2D` 內建的 `is_on_floor()`（那個是 `move_and_slide()` 專屬，這裡完全不呼叫）。**朝向**：`facing_dir` 依水平輸入翻轉，只套 `sprite.flip_h`（純視覺，血影沒有 hitbox 要跟著鏡像，不像 `VsPlayer` 要整個 `graphics` 子樹翻轉）。`velocity`/`grounded`/`facing_dir` 都進 `save_state()`/`restore_state()`（前兩者會影響後續幀模擬結果，`facing_dir` 雖純視覺但值本身是跨幀累積狀態，不存的話 rollback resync 後可能閃一下方向）。**`InputState.up_held` 欄位已移除**（改版後沒有消費端，維持它只是死欄位；封包 byte0 空出一個 bit）。

**附身期間紅色殘影拖尾**（讓玩家一眼看出現在操控的是血影，不是角色本體）：`VsBloodShadow._vfx_ghost()`，跟 `VsDodge` 的衝刺殘影同一套 `CombatManager.spawn_ghost()`（複製 `sprite` 外觀＋Tween 淡出，純視覺、真實時間淡出不影響確定性，跟 `VsPlayer.vfx_ghost()` 同一個既有慣例），固定紅色 `GHOST_COLOR`。由 `advance_visual()`（`vs_world._update_shadows()` 每幀呼叫，不管血影當下有沒有被操控都會跑）內部判斷 `owner_player.is_possessing` 才倒數 `_ghost_timer`（`GHOST_INTERVAL` 0.05s，跟 `VsDodge._ghost_timer` 同款）觸發，剛召喚、還沒轉移操控權那段期間不噴。`VsBloodShadow.is_resimulating`（`vs_world._simulate_frame()` 每幀同步，比照 `p1.is_resimulating`/`p2.is_resimulating`，不進快照）擋掉 rollback 重模擬期間的重複觸發；`_ghost_timer` 進快照。

**`CombatManager.spawn_ghost()` 擴充兩個選填參數**（`z_index_offset`/`rise_distance`，都預設 0——`VsDodge` 衝刺殘影等既有呼叫端完全不受影響，走原本「原地淡出」的分支）：血影的殘影要求「疊在血影本體後面」＋「淡出同時往上飄，不是原地消失」，`VsBloodShadow` 傳 `GHOST_Z_INDEX_OFFSET`（-1，血影本體 z_index 預設 0，殘影設更低的值就會畫在後面——兩者是同一個 `vs_world` 的直屬子節點/同一個 canvas layer，z_index 比較才有意義）／`GHOST_RISE_DISTANCE`（24px，淡出的 0.3s 內同步往上飄這段距離，`tween.parallel()` 讓位置位移跟透明度淡出同時進行）。

**`VsPlayer._recall_shadow()` 是銷毀血影的唯一入口**（三個觸發來源共用，回血邏輯只寫一次）：長按滿 1 秒／角色本體受到攻擊（`_on_hurtbox_hurt()` 最開頭，比 `is_defeated` 早退還早，呼叫完不 return，不影響這下攻擊接下來的傷害/硬直處理）／血影碰到懸崖類機關（`vs_world._check_hazard_for_shadow()`，只認 `HazardType.CLIFF`，規格沒提到的機關類型一律跳過不處理）。三者都呼叫這一個函式：`vs_world.despawn_blood_shadow()` 銷毀節點、`shadow=null`、`is_possessing=false`、`heal(SHADOW_DESTROY_HEAL)`（目前 100）。**血影本身沒有 hurtbox**——不掛任何會被 `_check_manual_hits()` 之類迴圈掃到的節點，天然滿足「不會受到任何攻擊判定」，不需要額外的無敵判斷。

`VsBloodShadow`（`VsMods/combat/VsBloodShadow.gd`+`.tscn`）是 `vs_world` 直屬子節點（同 `VsProjectile` 座標慣例），但**只有 0/1 顆、不是陣列**（`vs_world.p1_shadow`/`p2_shadow`）——rollback 還原（`_restore_shadows()`）整批砍掉重建（同 `_restore_projectiles()` 邏輯，但固定兩個欄位不是迴圈），還原後要把新節點接回 `VsPlayer.shadow` 參照（同 `VsProjectile.owner_player` 慣例）。懸崖判定重用 `_sim_rect_hazard()`，但血影自己的碰撞尺寸讀它 `CollisionShape2D` 子節點的 `RectangleShape2D`（`_sim_rect_shadow()`）；`VsHazard.gd` 新增獨立的 `_p1_shadow_overlapping`/`_p2_shadow_overlapping` 邊緣觸發追蹤（不能跟玩家本體共用同一組旗標——同一個 hazard 底下玩家跟自己的血影可能同時分別重疊/不重疊）。快照/checksum 都比照彈道併入。

**攝影機**：`VsPlayer.get_camera_target()`（`is_possessing` 時回傳 `shadow`，否則回傳 `self`）由 `vs_world._process()` 每幀推給 `camera.target_p1`/`target_p2`——純視覺、不進快照，跟該函式既有「每幀從模擬狀態推視覺更新」（`hud.update_timer()`）同一個模式。**單純把 target_p1/p2 換成血影是不夠的**——附身時角色本體不再被任何 target 代表，鏡頭會把停在原地的本體排除在畫面外（實測抓到的 bug：使用者反應「本體會被排在畫面外」）。修法：`VsCamera` 新增 `target_p1_extra`/`target_p2_extra`（附身時 `vs_world._process()` 額外塞本體 `VsPlayer` 進去，沒附身是 `null`），`_physics_process()` 的取景數學從「寫死兩點」改成收集所有非 null 的點（`target_p1`/`target_p2`/兩個 extra，最多 4 點）算**邊界框**（bounding box）取中點/跨距，取代原本的兩點平均/差值——**只有 2 點時數學上完全等價於原本版本**（`min(a,b)+max(a,b) == a+b`，中點/跨距算出來一樣），不影響已經調好的鏡頭手感（`follow_speed_x/y`/`zoom_speed`/`margin`/`min_zoom`/`max_zoom` 全部沿用），附身時 3 點才會把血影＋本體＋對手一起框進畫面。

明確不做（先做骨架，之後再細修）：血影移動範圍不 clamp 到場地邊界；只對懸崖機關生效，陷阱/彈簧/傳送矩陣/補包對血影一律無效；召喚/銷毀動畫與血影視覺都是骨架佔位，不是最終美術。

### VsMods 奈何武藝（移植自主遊戲 Boss 奈何橋）

奈何（`VsCharacterRegistry.CHARACTERS["Naihe"]`）的武藝來源是主遊戲關底 BOSS「奈何橋」（`enemies/boss/BossNaihe/`）自己的招式庫，**不是**另一把武器——Clotty 綁定 Katana，奈何綁定這個 Boss。Boss 招式庫（近戰 A1~A9、後跳、手砲、地刺）遠比 6 個武藝槽豐富，且有幾招用了 VsMods 目前沒有的機制（A7 黃圈技的彈反窗、A8→A9 的吸引拉近、A6/A4 的連續生成地刺）；2026-08-01 先完成前 4 招，逐招移植策略由使用者明確指定，5、6 待後續說明：

- **`Art_Naihe_1`（`attack_6`，地刺）**：唯一保留原版特殊機制的一招——「依序生成多顆判定」。原版 `NaiheSpikeSpawner` 用 `await` 迴圈依序生 5 根地刺，VsMods 禁用這種 real-time 等待（違反確定性規則），改成在 `art_1` 動畫軌道上直接排 5 個 `spawn_ground_spike(offset_x)` Call Method key（t=0.55/0.80/1.05/1.30/1.55，間隔 0.25s；offset_x=0/140/280/420/560，比照原版 `spacing=140px`），效果一致但完全落在「tracks over code」慣例內，之後想調間距/時機直接在動畫面板改 key。
- **`Art_Naihe_2`（`attack_8`）／`Art_Naihe_3`（`attack_7`）**：原版分別是「吸引拉近」（`pull_towards_owner` 拉近玩家再無縫接 A9）跟「黃圈技」（1.44s 前搖＋彈反窗，能被對策技破解）——使用者明確要求這次都先當一般招式，不用移植這兩個特殊機制（之後有需要再加）。各自沿用 Naihe 場景複製自 Clotty 時留下、本來沒被引用的 `Graphics/HitboxArt2`/`Graphics/HitboxArt3` 節點扛判定（大小/位置沿用，只改 `VsHitbox` 的 `damage`/`knockback`/`hitstun_time`，拿掉 Clotty 原本的 `combo_hits`/`sticky`/`break_level` 覆寫）。
- **`Art_Naihe_4`（`dash_back`）**：純後撤，使用者明確表示甚至不用判定框——後撤動能完全靠動畫軌道上的 3 個 `strike_impulse` Call Method key 驅動（數值直接照抄 Boss 原始動畫：`-500`/`-100`/`0.0` @ t=0.1/0.3/0.55），腳本本身跟其他三招同一套模板（`enter()` 播動畫、`IMPULSE_FRICTION` 煞停、動畫播完 `_recovery_transition()`），沒有任何後撤專屬程式碼。

四支腳本（`VsMods/player/arts/Art_Naihe_1~4.gd`）全部沿用 `Art_Clotty_3`/`Art_Clotty_4` 的「純模板」寫法，沒有 sticky 連段、沒有體質軌道（比照「武藝地板值已拿掉」的既有慣例，`anim_armor_tier` 維持預設 `NONE`，要不要給體質保護留給使用者之後自己在編輯器補軌道）。動畫（`NaiheAnimLib.tres` 的 `art_1`~`art_4`，`art_1` 是新增項目、`art_2`/`art_3`/`art_4` 是直接覆蓋掉原本 Clotty 動畫的複製品）texture/hframes/vframes/region_rect/frame-key 資料整組從 `enemies/boss/BossNaihe/BossNaihe.tscn` 的 AnimationPlayer 逐一比對移植，跟 Clotty 六招當初的移植方式一致；刻意**沒有**移植 Boss 原本的 `spawn_anim_vfx`/`vfx_action_sfx` 裝飾性軌道（呼叫簽名跟 VsMods 自己的 `spawn_anim_vfx` 對不上，貿然照抄參數數量會直接 Call Method 出錯，這幾個純視覺加分項先跳過，不影響招式本身能不能用）。

**`VsProjectile.gd` 新增 `lifetime: float`/`_age: float`**（`speed=0` 的靜止彈道專用，地刺是第一個用例）——`should_despawn()` 多一個 `lifetime > 0.0 and _age >= lifetime` 條件，跟既有的 `traveled >= max_distance` 並列，預設 0 不啟用、完全不影響 Art_Clotty_1/5 現有的飛行彈道。`vs_world.gd` 新增平行的 `spawn_ground_spike()`/`GROUND_SPIKE_SCENE`（`VsMods/combat/VsGroundSpike.tscn`，共用同一支腳本，只換 `Sprite2D` 貼圖成 `A3_e.png` 靜態單幀，比照既有彈道系統「少一個要顧 rollback 確定性的活動部件」的理由不搬 Boss 原本 9 格的「emerge」逐格動畫），生成邏輯跟 `spawn_projectile()` 同構，差異只在出手位置公式（地面高度，不是胸口高度的 -30 偏移）跟 `speed=0`。**`VsProjectile` 新增 `scene_kind: StringName`**（`"wave"`/`"spike"`，隨 `save_state()`/`restore_state()` 走）——rollback 還原時 `vs_world._restore_projectiles()` 靠這個決定該用哪個 `PackedScene` 重建節點（節點本身砍掉重建，`projectiles` 陣列裡混著兩種來源的物件，光看快照資料本身分不出來，必須額外存這個標記）。

**投射物數值改用 Inherited Scene 配置，不再手動複製整個場景**（2026-08-01，使用者反應「不知道去哪裡調投射物傷害」，加上未來還會有更多投射物招式）：`VsProjectile.tscn` 是基底（`VsHitbox` 子節點的 `damage`/`knockback`/`hitstun_time`/`hit_sfx_type`、根節點的 `speed`/`max_distance`/`lifetime` 都是預設值），新投射物視覺／數值直接在編輯器對它按「New Inherited Scene」，改 `Sprite2D` 貼圖跟 `VsHitbox` 數值即可，跟角色系統 `VsPlayerBase.tscn`→`VsPlayerClotty.tscn`/`VsPlayerNaihe.tscn` 同一套已經很熟的慣例，不用再手動複製整棵節點樹、也不用寫任何程式碼。`VsGroundSpike.tscn` 已經改成這個模式，示範供之後照抄：`[node name="VsProjectile" instance=ExtResource(...VsProjectile.tscn...)]` 當根節點，底下只列出真的被覆寫的子節點屬性（`parent="."`/`parent="VsHitbox"`，沒有 `type=`——Inherited Scene 的文字格式規則見上方「VsMods 選角色系統」一節）。⚠ 若新投射物的貼圖沒有基底那種染色/縮放需求，記得在 `Sprite2D` 覆寫區塊明確把 `modulate`/`scale` 重設回 `Color(1,1,1,1)`/`Vector2(1,1)`——不重設的話會沿用基底（劍氣彈道）自己的染色/縮放，貼圖會跟著變色/變形。
**投射物動畫是選用的，補回 `AnimationPlayer` 支援**（2026-08-03，使用者反應原版地刺的「破土逐格」動畫被拿掉了）：`VsProjectile.gd` 新增 `@onready var anim_player := get_node_or_null("AnimationPlayer")`——base `VsProjectile.tscn` 本身不放這顆節點（不是每個投射物都需要動畫，劍氣維持靜態單幀），哪個 Inherited Scene 變體想要動畫，就在編輯器對它新增一顆 `AnimationPlayer` 子節點＋一支固定叫 `"default"` 的動畫（名稱是寫死的約定，`VsProjectile.gd` 不用知道每個變體實際做了什麼動畫）。`_ready()` 比照 `VsPlayer._ready()` 把 `callback_mode_process` 設 MANUAL、`callback_mode_method` 設 IMMEDIATE（同一套確定性理由），有 `"default"` 就開始播；`simulate(delta)`（vs_world 每幀呼叫，正常幀與 rollback 重模擬幀走同一路徑）額外呼叫 `anim_player.advance(delta)`，動畫時間因此是模擬時間的純函數，跟 `VsPlayer` 的既有慣例一致。`_age`（原本只給 `lifetime` 判斷用）直接兼職當動畫時鐘，不用另外加欄位；`restore_state()` 還原後用 `_age` 做一次 `seek(..., true)` 對齊畫面，比照 `VsPlayer.sync_anim_to_state()`，`_resyncing_anim` 旗標也一併先準備好（目前地刺動畫只有值軌道，冪等安全，但之後如果哪個投射物動畫想加 Call Method 軌道，防呆已經在，不用重新設計）。`VsGroundSpike.tscn` 已經示範補回原版 `NaiheSpike.tscn` 的 9 格「emerge」動畫（`Sprite2D:frame` 值軌道，t=0~0.4 對應 frame 0~8，資料照抄來源，內嵌在 `.tscn` 自己的 `sub_resource`，沒有另外拆一份 `.tres`——單一小動畫沒必要比照玩家角色拆外部資源檔）。

`VsPlayer.gd` 新增 `spawn_ground_spike(offset_x, lifetime=0.4)`，跟 `fire_sword_wave()` 同一套防呆慣例（只擋 `_resyncing_anim`，不擋 `is_resimulating`——生成地刺是模擬狀態的一部分，rollback 重模擬要照樣真的生成）。

### VsMods 選場地系統

`VsMods/arenas/` — 場地跟角色同一套資料驅動設計：`VsArenaRegistry.gd`（id → `{display_name, scene_path}` 靜態表）+ `VsArena.gd`（場地根節點基底，`@export camera_limit_left/right/bottom` + `@export_group("鏡頭縮放範圍")` 的 `zoom_min`/`zoom_max`/`zoom_margin`，子節點固定用 `SpawnPoint_P1`/`SpawnPoint_P2` 兩個 Marker2D）+ 每張場地各自一個 `Arena_XXX.tscn`（例如 `Arena_Sunnyland.tscn`，含 `TileMap`/裝飾/`ParallaxBackground` 跟兩個重生點）。

`vs_world.tscn` 本身**不內嵌任何場地內容**，只留 `Camera2D`（掛 `VsCamera.gd`）跟一個空的 `ArenaRoot`（`Node2D`）掛載點。`vs_world.gd::_spawn_arena()`（`_ready()` 最前面呼叫，早於 `_spawn_players()`/`_spawn_round_manager()`）依 `VsGameManager.selected_arena_id` 查 `VsArenaRegistry` 動態 `instantiate()` 一份掛進 `ArenaRoot`，讀回 `VsArena.camera_limit_*` 設定 `Camera2D.limit_*`（平移邊界——鏡頭中心最多能移到哪）、讀回 `zoom_min`/`zoom_max`/`zoom_margin` 設定 `VsCamera.min_zoom`/`max_zoom`/`margin`（縮放範圍——兩人距離多遠時鏡頭最多拉多開/多近），並把 `spawn_point_p1`/`spawn_point_p2`（plain var，不是 `@onready` 固定節點路徑）指向剛掛進去的場地實例底下的 Marker2D。**這兩組數值一開始不對稱**：`camera_limit_*` 從一開始就是每張場地各自的 `VsArena` 欄位，但縮放範圍原本寫死在 `VsCamera.gd`（`vs_world.tscn` 唯一一份 `Camera2D`，不分地圖）——後來比照 `camera_limit_*` 的模式把縮放範圍也搬成每張地圖各自可調，`VsArena.zoom_*` 的預設值刻意跟 `VsCamera.gd` 原本寫死的數字一致，所以沒調過的既有地圖行為不變。想加新場地：複製一份 `Arena_XXX.tscn`（或全新設計，數值/collision 自己重新調）+ 在 `VsArenaRegistry.ARENAS` 加一筆資料，`vs_world`/`MapSelectScreen` 完全不用改。

### VsMods 場地機關（`VsMods/arenas/VsHazard.gd`）

懸崖/傳送矩陣/陷阱/彈簧/補包/buff包共用**單一類別**，`enum HazardType` 決定行為、各 `@export_group` 提供該類型專屬參數（跟 `VsHitbox` 用一堆分組覆蓋不同攻擊模式同一個慣例）。是場地場景（`VsArena` 底下）的固定節點，`vs_world._spawn_arena()` 掛完場地後用 `find_children("*", "VsHazard", true, false)` 收集（跟 `VsPlayer.hitboxes` 在 `_ready()` 收集所有 hitbox 子節點同一個模式），不是動態生成/銷毀的。判定不走 Area2D 訊號，`vs_world._check_arena_hazards()`（`_simulate_frame()` 內，`_check_manual_hits()`/`_update_projectiles()` 之後）每幀手動 `Rect2` 相交（`_sim_rect_hazard()` 用機關自己的 `position` 當錨點，跟玩家的 `_sim_rect(player.hurtbox, player)` 判斷重疊），**邊緣觸發**——只有「上一幀沒重疊→這一幀重疊」那一刻才觸發效果，一次接觸只算一次。

`VsHazard.gd` `extends Area2D`（跟 `VsHitbox`/`VsHurtbox` 同一個既有慣例）並掛一顆 `CollisionShape2D` 子節點——**純粹當「判定區大小」的視覺/編輯器 representation**（可以直接在 2D 視窗拖曳形狀控制點調整、會出現在「顯示碰撞形狀」除錯畫面），`_ready()` 把 `monitoring`/`monitorable` 都關掉，不會意外觸發任何 Area2D 訊號/物理運算。`vs_world._sim_rect_hazard()` 讀這顆子節點的 `RectangleShape2D` 實際尺寸做判定（跟 `_sim_rect()` 讀 hitbox 子節點同一招），沒有獨立的 `zone_size` 數字欄位。`VsMods/arenas/VsHazard.tscn` 是可直接實例化的樣板（Area2D+CollisionShape2D 已經組好，`collision_layer`/`collision_mask` 都設 0），放置一顆機關只需要拖這個場景進場地、調位置/形狀、在 Inspector 設定 `hazard_type` 跟對應參數。⚠ 第一版沒有 `CollisionShape2D`（純 `Node2D` + 一個 `zone_size` 數字欄位），使用者實測「開了顯示碰撞體也看不到判定區」才發現——純數字欄位在編輯器/除錯畫面完全沒有對應的視覺，之後任何新增的「判定區域」類節點都要記得掛一顆 `CollisionShape2D` 才有可視化，不能只給數字欄位。

- **傷害/擊退復用既有管線**：`VsPlayer.take_hazard_damage(damage, knockback, causes_knockdown, hitstun, bypass_invincibility)` 組出跟 `_on_hurtbox_hurt()` 一樣格式的 `pending_hit`，直接吃現有的 `_apply_pending_hit()`（硬直/擊飛/倒地判定完全沿用，不重新發明）。`bypass_invincibility=true` 給懸崖/傳送矩陣用（環境傷害，不能被無敵吸收）；陷阱/彈簧走 `false`，衝刺無敵可以躲開。
- **傳送矩陣可選傳送後樣式**：`VsHazard.teleport_launches`（bool）——`true` 走 `take_hazard_damage()` 一般命中管線（`causes_knockdown=true` + `teleport_knockback.y<0` 觸發 `VsLaunched` 弧線飛行，可被追打）；`false`（預設）直接 `player.take_damage()`（純扣血，不進 `pending_hit`，不強制任何狀態，新位置直接一般重力墜落、落地就站穩沒有硬直）。
- **⚠ `apply_spring()` 呼叫順序踩過的坑**：`VsJump.enter()` 會無條件把 `velocity.y` 蓋成 `effective_jump_force()`（一般跳躍力道）——第一版 `apply_spring()` 先設 `velocity.y = force` 再 `transition_to(&"vsjump")`，結果轉場時 `VsJump.enter()` 立刻把 `force` 蓋掉，`spring_force` 設多少都沒差（實測 10 跟 1000 效果一樣）。正確順序是**先轉場再設 velocity**：`state_machine.transition_to(&"vsjump")` 之後才 `velocity.y = force`，讓 spring 的值蓋在 `VsJump.enter()` 的預設值之上而不是反過來。任何「先轉場進某個狀態、再想覆寫該狀態 `enter()` 會設的欄位」的新功能都要注意這個順序陷阱。
- **懸崖死亡**：`VsPlayer.kill_instantly()` 只把 `hp` 設 0，`VsRoundManager._tick_fighting()` 既有的 `hp<=0` 判定自動接手死亡演出（強制進 `VsKnockdown`、回合結束），不用另外處理。
- **buff 包的確定性隨機**：`VsPlayer.apply_random_buff(seed_value)` 用 `RandomNumberGenerator` 播種，種子傳 `VsNetworkManager.get_game_frame()`（雙端已同步的資料）——任何影響模擬結果的亂數都要用共享種子，不可各自獨立初始化（CLAUDE.md 確定性規則表）。效果存進 `hazard_buff_mult: Dictionary`（`stat_name → 倍率`，`VsRoundManager._reset_round()` 清空，跟 `ukemi_uses_left` 同待遇），**不直接改 `move_speed`/`jump_force` 等 `@export` 欄位**（會被 `apply_arts_bonus()` 換裝武藝時的重算覆寫）——讀取點一律走 `effective_move_speed()`/`effective_air_speed()`/`effective_jump_force()`（或內部的 `_buffed(stat_name, base_value)`），不能直接讀裸欄位。
- **平台（單向下拉）——唯一不是觸發區域、是碰撞層級機制的一種**：Godot 原生 `CollisionShape2D.one_way_collision` 本來就會被 `move_and_collide()` 尊重（不限 `move_and_slide()`），所以「站上去/從下面穿過去」不用寫程式碼——放 `StaticBody2D`（`one_way_collision=true`）、`collision_layer` 設成 `project.godot` 新增的 `2d_physics/layer_13="VsPlatform"`（位元值 4096）即可。只有「按下＋跳躍主動下平台」需要程式碼：`VsPlayerBase.tscn` 根節點 `collision_mask` 是 `4224`（`128|4096`，地板層+平台層）；`VsPlayer._move_deterministic()` 每幀依 `platform_pass_through_left > 0` 動態重算 `collision_mask`（不是「記住原值再恢復」，跟專案「無狀態、每幀依資料重算」慣例一致）；`VsPlayerState._check_platform_drop(input)`（`is_crouch and jump and _grounded()`）在 `VsIdle`/`VsRun` 的 `input.jump` 判斷之前呼叫，消費掉輸入、不觸發一般跳躍。
- **階梯輔助**：用磚塊拼成樓梯/弧形地形時，每一階的垂直轉角本身就是一道牆，`move_and_collide()` 分不出「矮到可以直接踏上去的階梯」跟「真的牆」，水平移動會卡住（使用者實測抓到：弧形階梯拼出來會卡腳）。`VsPlayer._try_step_up(motion)`（`_move_deterministic()` 開頭、貼地且水平有位移時呼叫）：純水平方向先 `test_move()` 探是否被擋 → 沒擋到就不用墊、交還一般流程；擋到的話再驗證墊高 `STEP_HEIGHT`（18px，略大於一格磚塊 16px）後頭上淨空、且墊高後水平方向也淨空，兩項都過才是真的階梯（比真牆矮）——真牆因為墊高後仍被擋住，兩項測試會擋下，行為不受影響。**不是判定通過就一幀瞬間套用**（那樣水平只移動一幀份距離、垂直卻瞬移一整格高度，視覺上是明顯的「一格一格跳」割裂感，使用者實測反饋要求改掉）：判定通過後用跟原本相同的「墊高→水平跨一整格（`STEP_WIDTH`=16px）→貼地」邏輯算出終點座標（複用既有判斷保證落點正確），但不直接套用，改存進 `_step_climb_from`/`_step_climb_to`，接下來幾幀由 `_advance_step_climb()` 對 `position` 做線性插值——爬升所需時間用當下水平速度換算（`STEP_WIDTH / |velocity.x|`），水平視覺速度爬階梯時不會變慢，只有垂直被拉開成連續斜線。插值期間刻意繞過碰撞（直接寫 `position`，不呼叫 `move_and_collide`）——終點已經驗證過合法，中途姿勢在幾何上跟階梯垂直面重疊是預期內、不影響結果。`_step_climb_t/_from/_to/_duration` 都是會影響後續幀結果的模擬狀態，進 `save_state()`/`restore_state()`。**下樓梯是對稱的另一半（`VsPlayer._try_step_down(motion)`）**：上樓梯有「水平被擋住」這個碰撞事件可以當觸發點，下樓梯沒有——原本純靠重力自然墜落，每一階都會有一小段自由落體再貼地的頓挫感（使用者實測：「下樓梯每一階都是用下落的」）。判斷邏輯反過來：這幀水平移動沒被擋住、但移動後那個 x 位置原地懸空（`test_move(0,0.1)` 沒貼地）、且往下 `STEP_HEIGHT` 內找得到地板（落差超過 `STEP_HEIGHT` 就判定是真懸崖，讓重力自然墜落，不受影響）——三項都成立才是合法的一階落差，一樣用 `_step_climb_*`/`_advance_step_climb()` 插值處理，不讓重力介入。兩個函式共用同一組插值狀態欄位，`_move_deterministic()` 依序嘗試 `_try_step_up()` 再 `_try_step_down()`（兩者觸發條件互斥，同一幀只會有一個生效）。
- **快照**：`hazard_buff_mult`/`platform_pass_through_left` 進 `VsPlayer.save_state()`/`restore_state()`；`vs_world._save_snapshot()`/`_restore_snapshot()` 新增 `"hazards"` key，**按索引還原**（機關是固定節點、順序不變，不像彈道要整批砍掉重建）。`vs_world._on_round_started()` 呼叫每顆 `VsHazard.reset_for_round()`（補包/buff包重新可用，跟彈道清理同一類「攻擊洩漏」防護，放在 `is_resimulating` 擋掉之前）。

⚠ **這套系統目前刻意沒有在任何場地放實際機關實例**——`Arena_Sunnyland.tscn`/`Arena_ExperimentSpace.tscn` 都是封閉地板，懸崖需要真的挖空的關卡設計，屬於使用者自己的美術/關卡範疇。之後要用時，直接在場地場景裡放 `VsHazard` 節點（設定 `hazard_type`/`zone_size`/對應參數）或 `StaticBody2D`+`one_way_collision` 平台即可，`vs_world`/`VsPlayer` 端完全不用再改。

選場地畫面 `VsMods/ui/MapSelectScreen.gd`/`.tscn`——`SelectScreen`（角色/武藝）確認後的下一步，取代原本直接進 `vs_world.tscn`。**第一版做過鍵盤方向鍵選+action鍵確認/取消（比照戰鬥操作鍵位），使用者反映不直覺，改成純滑鼠點擊選圖 + 兩顆固定的「確認」/「取消」按鈕**，`VsMods/ui/MapSelectScreen.tscn` 裡的真實節點，不再套用 InputState 那條「本機永遠用 P1 配置」的鍵盤讀取規則（改回全專案其他選單畫面共通的滑鼠互動）。

**離線模式（本地雙人）刻意不分 P1/P2 面板**——跟 `SelectScreen` 的角色/武藝選擇不同，選地圖的目標本來就是雙方要選到同一張，兩位玩家就在同一台電腦前，用嘴巴講好選哪張即可，不需要重複整套「雙方各自確認再比對」的流程；離線模式點一張地圖、按確認就直接進場，沒有「等待」狀態，取消鍵也不會顯示（`_cancel_button.visible = _is_online`）。連線模式才走完整的確認/取消/比對流程：單一本機 `_confirmed_id` 透過 `VsNetworkManager.send_arena_choice(arena_id, confirmed)`/`remote_arena_received(arena_id, confirmed)`（跟武藝選擇 `send_arts()` 同一套 WS JSON relay，`type="arena"`，見上方「Signaling server」一節的白名單注意事項）跟對方同步，雙方都確認且相同才寫入 `VsGameManager.selected_arena_id` 並轉場。**每次點選（不是只有確認）都會廣播**（`confirmed=false`），對方畫面上有個 `RemoteMarker` 標籤即時跟到你目前選的那顆按鈕下方，顯示「對方選擇中」/「對方已確認」，不用乾等到對方按下確認才知道他在看哪張。

### VsMods 回合流程（`VsRoundManager.gd`）

三戰兩勝，`Phase` 除了戰鬥/回合結算，還有 `Phase.ARTS_RESELECT`（血量見底進下一輪前，雙方可替換最多 2 種已裝備武藝）：

- **回合間重選武藝**：固定 `ARTS_RESELECT_DURATION`（目前 30 秒）倒數 + 雙方都按確認鍵可提早結束。**確認鍵走 `InputState`（塞進 2 bytes 封包 byte 0 的空位），不是 WS 訊息**——因為「兩端何時都按下確認」本身會影響模擬幀轉場時機，若靠 WS 訊息到達的真實時間觸發，兩端網路延遲落差會導致轉場發生在不同模擬幀 → checksum 分歧；改走 `InputState` 就跟其他輸入共用同一條延遲+預測+rollback 修正管線，天生同步。`VsRoundManager.p1_confirmed`/`p2_confirmed` 因此**要**進 `save_state()`/`restore_state()`。UI 按鈕點擊透過 `VsGameManager.pending_confirm_1`/`pending_confirm_2` 一次性旗標橋接，`InputState.from_input()` 下次讀取時消費掉。**線上模式本機面板的確認鍵永遠要設 `pending_confirm_1`**，不能照面板是哪一側決定——本機輸入固定走 `from_input(1)`（見上方那條全域規則），`from_input(2)` 在線上模式永遠不會被呼叫，設成 `pending_confirm_2` 會讓確認鍵在本機是 CLIENT 時完全沒反應。
  - 「原始裝備武藝」的比對基準要讀 `VsPlayer.art_slots`（活的、隨 `reload_arts()` 更新），不是 `VsGameManager.p1_arts`/`p2_arts`（那份只在賽前 `SelectScreen` 寫一次，中途重選不會更新）。
  - `VsPlayer.reload_arts(new_slots)` 只能在 `transition_to(&"vsidle")` 之後呼叫（保證 `current_state` 不是任何 `VsMartialArt` 節點）。**⚠ Godot 同名節點命名陷阱**：卸載舊武藝節點時必須先 `state_machine.remove_child(old)`（同步立刻釋放名稱）再 `old.queue_free()`——只呼叫 `queue_free()` 的話，舊節點要到這一幀結束才真正離開場景樹，這一幀稍後 `_load_arts()` 馬上 `add_child()` 一個同名新節點會被 Godot 自動改名（例如 `"VsArt1@2"`），導致 `register_state()` 用錯誤的 key 登記，`transition_to("vsart1")` 找不到狀態。任何「同一幀內刪除再新增同名節點」的場合都要記得這個順序。
- **回合重置要無條件清空判定框/彈道**（`_reset_round()`）——sticky 連擊在「同一場對局內」刻意放行讓它跑完，但「回合」是更高一層邊界，上一回合沒打完的連擊/飛在空中的彈道不該延續到下一回合。`vs_world._on_round_started()` 同步清空 `projectiles`；彈道清理**不受** `is_resimulating` 擋（是模擬狀態的一部分，resim 要照跑），HUD 更新才受擋。
- 返回大廳分兩種等待：暫停選單主動點「返回大廳」立即切場景（`instant=true`）；對方棄權/斷線/desync 等**被動**離場保留 2 秒說明訊息（讓玩家看懂發生什麼事）——`vs_world._leave_to_lobby(msg, instant)`。
- 滑鼠鎖定邏輯統一收進 `vs_world._process()`（比照主遊戲 `Player.gd._process()`）：`overlay_open`（任何暫停/選單/重選浮層開著）或按住 `KEY_ALT` → `MOUSE_MODE_VISIBLE`，否則 `MOUSE_MODE_CAPTURED`。任何新增的全螢幕浮層都要記得加進 `overlay_open` 的判斷條件。
- **回合時限**（2026-07-27）：`VsRoundManager.ROUND_TIME_LIMIT`（60 秒），`_tick_fighting(delta)` 每幀遞減 `round_time_left`，時間到（且雙方都還沒死）比照死亡判定同一套 `winner` 慣例分流：血量多的一方獲勝，血量相同算平手（`winner=0`，雙方都各得一分，跟「兩人同時倒地」共用同一段程式碼，不用另外處理）。時間到**不會**強制任何一方進 `VsKnockdown`——沒有人真的被打死，維持原本站姿/動作。UI 顯示：`BattleHud._timer_label`，正中央、跟勝場點同一列，`vs_world._process()` 每幀 `hud.update_timer(round_manager.round_time_left)` push 進去（跟 HUD 讀 `_p1.hp` 血量數字同一種「vs_world 主動推」/「BattleHud 自己讀」搭配模式，這裡因為 `BattleHud` 沒有直接持有 `round_manager` 參照，改用 push）。勝場點原本中間只留 4px 縫隙（塞不下數字），改用 `TIMER_GAP`（12px）加寬，兩側點往外讓開。
- **回合結束不是「凍結」，是餵中立輸入**（2026-07-27，使用者要求「保持原有的動作以及物理」）：`_tick_fighting()` 偵測到死亡/時間到後，`phase` 變成 `ROUND_END`（`ROUND_END_DELAY` 2 秒），這段期間 `vs_world._simulate_frame()` 仍然對兩位玩家呼叫 `apply_input()`（物理/動畫/狀態轉場照常跑，例如倒地→彈起→起身能自然播完），只是餵一份全空的 `InputState.new()` 取代玩家實際輸入——不接受任何新操作，但既有動作/物理不中斷。這跟 `ARTS_RESELECT`/`GAME_OVER` 兩個階段不同：那兩個是選單/結算畫面，維持原本「完全跳過 apply_input()，只推進動畫避免畫面凍結」的做法。`ROUND_END_DELAY` 這幾秒是刻意留白的視覺優化窗口（之後要加運鏡/終結震動之類的都放這裡）。
- **敗北動畫分流**（2026-07-30，接續上一條）：死亡當下依 `current_state` 分兩條路，都在 `VsRoundManager._route_defeat()` 決定：
  - **硬直死亡**（`current_state is VsHurt`，或任何其他非 `VsLaunched`/`VsKnockdown` 的狀態——例如霸體吸收硬直但傷害致命）→ 直接 `transition_to(&"vsdefeated")`，完全跳過整套倒地流程，播新終局狀態 `VsMods/player/states/VsDefeated.gd` 的 `"did"` 死亡動畫，播完定格（不會再轉場，回合重置由 `_reset_round()` 強制轉回 `vsidle`）。**角色動畫庫要自己補一支叫 `"did"` 的動畫**，沒補的話 `get_animation("did")` 回傳 `null` 會報錯（跟 `launched_3` 當初一樣的坑）。
  - **擊飛死亡**（`current_state is VsLaunched`，或已經在 `VsKnockdown` 流程中）→ `_route_defeat()` 不介入，讓落地行程照舊跑（`launched_start`→`launched_3`→彈起→二次落地）；差別只在 `VsKnockdown.gd` 的 `bounced` 分支——二次落地時若 `VsPlayer.is_defeated` 為真，不再接 `&"vsgetup"`（起身動畫 `launched_2`），改成停在原地播 `LYING_ANIM`（`launched_3`）定格，即「最後的起身動畫換成躺地動畫」。
  - `VsPlayer.is_defeated`（死亡當下設定）/`defeat_settled`（終局狀態真正播完/定格那一刻設定）兩個旗標都進 `save_state()`/`restore_state()`（rollback 安全），每回合由 `_reset_round()` 重置。
  - **`ROUND_END_DELAY` 的 2 秒倒數改成等敗北動畫播完才開始**：`_tick_round_end()` 先呼叫 `_defeat_wait_done()`（沒死的一方自動過關；死亡的一方要等 `defeat_settled` 為真）擋住，兩人都過關才真正倒數 2 秒——不是從偵測死亡那一刻就固定倒數，避免死亡動畫被腰斬。**時間到觸發的 `ROUND_END`**（`_time_based_end` 旗標）沒有人死亡、不用等任何動畫，直接倒數（使用者明確要求「因時間敗北就不必動畫」）。
  - `VsDefeated` 不是場景固定子節點——跟武藝系統同一套動態註冊模式（`VsStateMachine.register_state()`），差別是它在 `VsPlayer._ready()` 就無條件註冊一次（不像武藝要等 `art_slots` 才知道掛哪支腳本），所有角色自動共用，不用逐一在 `VsPlayerBase.tscn`/各角色 derived 場景加節點。
  - **敗北期間「打不動」跟「無敵體質視覺」是分開的兩件事**——使用者反應「死後還顯示無敵光效」觀感很怪。`VsPlayer._on_hurtbox_hurt()` 用獨立的 `is_defeated` 旗標在最前面早退（回合結束等敗北動畫播完這段期間，殘留判定框仍可能命中，需要無視），刻意不透過 `get_armor_tier() == ArmorTier.INVINCIBLE` 這條路（會連帶觸發無敵體質描邊）；`_update_status_outline()` 也追加 `and not is_defeated` 直接整組跳過描邊，不管底層 `invincible_time_left`（`VsKnockdown.INVINCIBLE_COVER` 落地保護機制）是否仍非零——體質描邊是「還在打」的視覺語言，死掉的角色維持普通外觀即可。
- **敗北運鏡（2026-08-01）**：只有「非時間到、有單一勝負」的死亡才觸發（時間到、或雙方同時倒地平手，退回原本樸素的等待+`ROUND_END_DELAY`）。`VsRoundManager.RoundEndStage { PLAIN, LOSER_CLOSEUP, WAIT_LANDING, WINNER_CLOSEUP }` 取代原本單純的「等敗北動畫+固定倒數」，`round_end_stage`/`round_end_winner` 進 `save_state()`/`restore_state()`（影響模擬結果，rollback 必存）：
  - `LOSER_CLOSEUP`（固定 `CLOSEUP_DURATION` 2 秒，不等動畫）：特寫敗方 + 整場模擬用 `TIME_SLOW_RATE`（0.15，2026-08-01 從 0.25 調低）慢動作播放——死亡/倒地動畫這時應該正在慢動作播。鏡頭拉近倍率 `vs_world.DEFEAT_CLOSEUP_DEPTH`（1.5，同日從 2.0 調低）。
  - `WAIT_LANDING`：退出特寫+恢復正常速度，這裡才真正等 `_defeat_wait_done()`（敗北動畫播完）。
  - `WINNER_CLOSEUP`（固定 2 秒）：特寫勝方，時間到直接接「比賽結束/進重選」收尾——取代原本的 `ROUND_END_DELAY`，不會兩個都算一次。
  - **慢動作不用 `Engine.time_scale`**（VsMods 全域禁用，見確定性規則表）：`VsRoundManager.sim_delta_scale()` 純粹是 `phase`/`round_end_stage` 的確定性函數，`vs_world._simulate_frame()` 只把「餵進 `apply_input()`/`_check_manual_hits()`/`_update_projectiles()`/`_check_arena_hazards()` 的 delta」乘上這個縮放值（`round_manager.tick()` 本身仍用原始未縮放 delta，子階段計時器的「2 秒」不會被慢動作拉長）——角色動畫/位移/打擊判定/彈道/機關全部一起變慢，不只敗方，兩端算出的縮放值保證一致，rollback 安全。
  - 鏡頭特寫走 `VsRoundManager.round_end_stage_changed(stage)` 訊號（跟 `round_ended`/`round_started` 同一套慣例：訊號在 resim 期間一樣會重複 emit，防重複的責任在 `vs_world._on_round_end_stage_changed()` 開頭的 `is_resimulating` 檢查，不在訊號發送端），對 `round_end_winner` 指向的那個 `VsPlayer` 呼叫既有的 `vfx_closeup(2.0, VsRoundManager.CLOSEUP_DURATION)`（`VsCamera.cinematic_zoom()` 自己的計時器到時自動交還一般立回模式，不用另外呼叫「結束特寫」）。
  - **場地機關（懸崖）死亡跳過敗方特寫**：`VsPlayer.died_from_hazard`（`kill_instantly()` 設定，`_reset_round()` 重置，進 `save_state()`/`restore_state()`）——`kill_instantly()` 沒有正常命中/硬直可看，特寫敗方沒意義。`_tick_fighting()` 判斷勝負後，若敗方 `died_from_hazard`，直接把 `round_end_stage` 設成 `WAIT_LANDING`（跳過 `LOSER_CLOSEUP`，連帶跳過慢動作，因為 `sim_delta_scale()` 只在 `LOSER_CLOSEUP` 生效），純等敗北動畫（`"did"`）播完就進 `WINNER_CLOSEUP`——只保留勝方特寫。目前只有懸崖用 `kill_instantly()`，其餘機關（陷阱/傳送矩陣）走 `take_hazard_damage()` 一般命中管線，有正常的受擊/擊飛動畫，不受這條影響。

### VsMods 音效

沿用主遊戲既有的 `AudioManager` autoload（`sound/AudioManager.gd`），VsMods 沒有另外蓋一套音效系統。`VsPlayer.gd` 三個包裝函式，跟 `vfx_*` 系列同一套 `_vfx_blocked()` rollback 防呆：`vfx_sfx(stream, volume_db, pitch_scale)`（`play_sfx` 包裝）、`vfx_action_sfx(key, volume_db=-8.0)`（`play_action_sfx` 包裝，等同主遊戲 `trigger_swing_sfx`，可直接當動畫 Call Method 軌道呼叫）、`vfx_hit_sfx(hb)`（`play_hit_sfx` 包裝，讀 `VsHitbox.hit_sfx_type`）。`vs_world._manual_check()`/`_manual_check_projectile()` 命中判定呼叫 `vfx_hit_sfx`，跟 `vfx_spark`/`vfx_shake` 同一個位置、同一個原則（不管防禦/霸體/無敵吸收與否都給回饋）。

**BGM——每張場地固定配一首**：`VsArena.gd` 新增 `@export_group("音效設定")` 底下的 `arena_bgm: AudioStream`／`bgm_volume: float`（預設 -10.0），跟主遊戲 `Worlds/world.gd::level_bgm` 完全同一套模式，同樣呼叫 `AudioManager.play_bgm()`/`stop_bgm()`，VsMods 沒有另開一套播放邏輯。`vs_world._spawn_arena()` 掛完場地後、確認 `arena.arena_bgm` 非空才播放（留空＝這張地圖暫時沒配樂，不強迫每張都要有）；播放時機只在進場一次（不是每回合），回合切換不會重播。兩個離開 `vs_world` 的路徑都呼叫 `AudioManager.stop_bgm(1.0)`（1 秒淡出）：`_leave_to_lobby()`（暫停選單主動離場、棄權/斷線/desync 被動離場都會經過這裡）、`_unhandled_input()` 比賽結束後任意鍵返回選角那個分支（沒有經過 `_leave_to_lobby`，是獨立的 `change_scene_to_file` 呼叫，要另外補一次 `stop_bgm`）。**音樂檔案本身要使用者自己在編輯器 Inspector 指定**（`Arena_Sunnyland.tscn`/`Arena_ExperimentSpace.tscn` 目前都留空）——跟場地美術/機關擺放同一套「我做框架、使用者做內容」分工，不要自己亂猜一首主遊戲已經在用的曲子塞進去（`sound/BGM/` 底下現有的兩首已經被 `home.tscn`/`cave.tscn`/`title_screen.tscn` 用掉，不是給 VsMods 用的空閒資源）。

### VsMods 防禦分階段 + 受身（全角色通用防禦技）

`VsGuard.gd` 按住循環播 `defense`（強霸體）、放開播一次性後搖 `defense_2`（**後搖無霸體**——`VsGuard.is_blocking()` 只有按住階段回傳 true，`VsPlayer.get_armor_tier()`/`_on_hurtbox_hurt()` 的 `cur is VsGuard` 分支都先檢查這個，後搖期間視同 `ArmorTier.NONE`，會被打斷走一般受擊流程）。

**全角色通用命中回饋規則**：`VsPlayer.HitOutcome { NORMAL, GUARDED, INVINCIBLE }` + `last_hit_outcome`（同幀執行期通訊，不進快照），`_on_hurtbox_hurt()` 在完美閃避/`is_invincible()`/格擋成功（非強破霸）三個分支各自標記。`vs_world` 命中後三分支處理：`INVINCIBLE` → 完全不給回饋（無音效無火花，震動不受影響）；`GUARDED` → 火花換成 `VsPlayer.vfx_block_spark()`（BLUNT 火花，貼防禦方身上朝防禦方面向，跟一般命中火花是不同視覺語彙）；其餘維持「不管防禦/霸體/無敵與否都給回饋」的既有邏輯。

**受身**（VsMods 原創設計，非移植自主遊戲）：`VsHurt`（受擊硬直）或 `VsLaunched`（擊飛）這兩個「無法操作」狀態期間，按技能鍵觸發 `VsPlayer.try_ukemi()`——**主動打斷當下狀態**（`velocity` 歸零、`VsLaunched` 直接 `return &"vsfall"`、`VsHurt` 直接 `return _recovery_transition(input)`，跳過剩餘硬直/`knockdown_after` 強制倒地分流），不是被動加防護。觸發後進入 `UKEMI_COOLDOWN`（10 秒）冷卻，不是次數限制（2026-07-27 從「每回合限 2 次」改成「無限次數、10 秒冷卻」）——`ukemi_cooldown_left` 每回合在 `_reset_round()` 重置成 0（重新可用），冷卻中`try_ukemi()` 直接擋掉。給 2 秒強霸體（複用既有 `post_dash_armor_left` 機制）、播黃色十字特效+音效。`BattleHud` 的顯示也跟著從「次數點」改成跟血條/能量條同一套 `_make_bar()`/`_set_bar()` 的填充冷卻條（`_p1_ukemi_bar`/`_p2_ukemi_bar`），視覺上「還要等多久」比點數更直覺。⚠ 第一版做成「不取消狀態、只疊霸體」被使用者實測推翻（「按了根本沒用」）——這類「防禦性技能該擋什麼」的機制，設計時要先問清楚是「取消當下狀態」還是「只加保護後續攻擊」，不要照字面「獲得霸體」自己腦補。

受擊動畫在 `hurt`/`hurt_2` 兩支之間隨機挑一支（`VsHurt.enter()` 用未共享種子的 `randi()%2`——純視覺、硬直時長固定讀 `queued_hitstun` 跟動畫無關，安全；選擇結果存進快照，只在 `enter()` 骰一次，避免 rollback resync 中途重骰造成畫面跳變）。

### VsMods 武藝打斷規則

**衝刺可打斷任何武藝，但每支武藝的 `physics_update()` 都要自己接這行檢查**（`VsMartialArt.gd` 沒辦法統一生效，因為每支武藝完全覆寫 `physics_update()`、不呼叫 `super`）：`if input.dodge and vs.use_dash_energy(30.0): return &"vsdodge"`，放在函式最前面。新增武藝時務必手動加這行（已經漏過不只一次）。

**武藝打斷普攻只能發生在連段窗口內**（`VsAttack.gd`/`VsAirAttack.gd` 的 `can_combo` 開啟期間），不是任意時點——舊規則「武藝可在普攻施放期間任何時點打斷」已拿掉，因為空中普攻在任意幀取消進武藝是「無限空中連段」bug 的根因之一。`_check_art_cast()` 呼叫從獨立的打斷優先級改成包進 `if vs.can_combo:` 區塊。**不影響** `VsIdle`/`VsRun`/`VsJump`/`VsFall`/`VsSkill`（這幾個不是「普攻」，隨時可放武藝）。

### VsMods 專屬設定選單

`VsMods/ui/VsSettingsPanel.gd`（全程式碼建立節點，無 `.tscn`）——刻意做成精簡版而不是直接沿用主遊戲 `settings_panel.tscn`（那份混了很多跟 VsMods 無關的選項）。只放 3 項：霸體輪廓開關、震動開關（都直接讀寫 `Game.config_*`，跟主遊戲共用同一份持久化設定，不是 VsMods 自己另開一份）、「更改按鍵」按鈕開 `VsKeybindMenu`。

`VsMods/ui/VsKeybindMenu.gd`（同樣全程式碼、無 `.tscn`）——跟主遊戲 `ui/KeybindMenu.gd` 是同一套機制（InputMap 動態綁定 + ConfigFile 持久化），**刻意不共用同一支腳本**（避免改動風險外溢到主遊戲設定選單），但共用同一個 `user://keybindings.cfg` 檔案跟 `"Controls"` section——因為 key 用 action 名稱當前綴，VsMods 的 `p1_*`/`p2_*` 動作名稱天生不會跟主遊戲的 `move_left`/`attack` 撞。「還原預設」只清 VsMods 這份清單涉及的動作，不像主遊戲的還原鈕整個 `keybindings.cfg` 砍掉重來。

**⚠ `globals/game.gd::_load_custom_keybindings_at_launch()` 的 `actions` 清單要跟 `VsKeybindMenu.ACTIONS_P1`/`ACTIONS_P2` 保持同步**——這是開機時把 `user://keybindings.cfg` 存的自訂鍵重新套用回 `InputMap` 的清單，之前只列主遊戲動作、不含任何 `p1_*`/`p2_*`，導致在 `VsKeybindMenu` 改的按鍵存檔正確但**重開遊戲後會消失**（開機從沒讀過那些 key）。兩處都有寫註解互相提醒，之後改一邊要記得同步另一邊。

`vs_world.gd` 暫停選單在「繼續」跟「返回大廳」中間插「設定」按鈕，用獨立 `CanvasLayer`（layer 21，蓋在暫停選單 layer 20 之上）承載 `VsSettingsPanel`，關閉時整層 `queue_free()`。

### VsMods 局內 HUD（`VsMods/ui/BattleHud.gd`）

主要靠程式碼建立節點（`_build_ui()`），例外是頭頂身份標籤 `VsMods/ui/VsNameTag.tscn`（見下）——外觀需要頻繁微調的 UI 元素改走可視化編輯，不要重蹈之前 `VsArtButton`/`VsArtInfoPopup` 那種「先寫死常數→使用者不滿意→回頭轉可視化編輯」的來回。

- **武藝能量顯示縮放**：`VsGameManager.ARTS_ENERGY_DISPLAY_SCALE`（目前 0.1）——底層 `VsPlayer.arts_energy`/`VsMartialArt.energy_cost` 數值不變，玩家看到的數字統一乘這個比例。能量條數字、徽章耗能數字、選角彈窗（`VsArtInfoPopup`）說明文字全部要套同一個常數，唯一權威來源放這裡，不要各自維護一份。
- **裝備武藝徽章**（局內顯示 3 槽裝備+耗能）：黑底白框（有裝備=白框、空槽=暗灰框）+ 招式正式名稱的第一個字（`VsGameManager.get_art_badge_char()`），統一黑白配色，不靠顏色分辨招式。按下武藝修飾鍵（E）時徽章放大高亮+耗能數字淡入，數值比照主遊戲 `player/UI/CombatUI.gd::_on_player_martial_mode_changed()`（0.12s/0.15s tween，`Vector2(1.15,1.15)`）。施放成功時的脈衝殘影比照 `CombatUI._spawn_cast_afterimage()`（同尺寸色塊放大 1.5 倍同時淡出，0.2s/0.4s），由新增的 `VsPlayer.art_cast(slot)` signal（`_check_art_cast()` 施放成功時 emit，套 `_vfx_blocked()` 防呆）觸發。單字字體可換：`BattleHud.BADGE_FONT_PATH`（`res://VsMods/ui/badge_font.ttf`）若存在檔案就自動套用，不存在就退回專案預設字體。
- **能量滿動態彩色**：`arts_energy >= max_arts_energy` 時能量條用 `Color.from_hsv()` 連續變色相（`Time.get_ticks_msec()` 驅動）——這是 `BattleHud._process()` 自己的純視覺邏輯，不是 `_simulate_frame` 呼叫鏈內的東西，不受確定性規則約束，可以放心用真實時間。
- **頭頂身份標籤**（`VsMods/ui/VsNameTag.gd`/`.tscn`，P1/P2 文字 + 指向三角圖標）——核心技術是「跟隨世界座標但不受鏡頭 zoom 影響」：`BattleHud` 每幀用 `get_viewport().get_canvas_transform() * vs.vfx_anchor_position()` 把玩家世界座標換算成螢幕像素座標（這個 transform 自動把 `VsCamera` 當下動態縮放算進去），呼叫 `VsNameTag.set_anchor_screen_position()` 只搬動這個 CanvasLayer 底下節點的**位置**，字體/圖標大小永遠不受鏡頭 zoom 影響。文字/圖標的相對位置、大小、字型、顏色、浮動速度/振幅（`float_speed`/`float_amplitude`/`phase` 三個 `@export`）全部在 `.tscn`/Inspector 裡可視化調，`.gd` 只負責浮動動畫邏輯跟接收位置。
- 血條數字只顯示單一數字（無 `/max`）；武藝能量條數字維持專案預設字體（不套 `badge_font.ttf`，只有徽章單字套）。

### 連線模式的完整測試清單（每次改動賽前流程都要重跑一遍）

`SelectScreen` → `MapSelectScreen` → `vs_world` 這條鏈的任何一段改動，離線模式相對好驗證，**線上模式的驗證成本高但不能省略**：本機一律用 P1 按鍵配置操作（見上方 InputState 章節）、WS 訊息型別要在 `signaling_server.py` 白名單裡且伺服器已重新部署（見上方 Signaling server 一節）、雙方轉場時機是否同步（尤其任何會影響模擬幀對齊的邏輯，例如 Phase 9 補完那次「回合確認鍵改走 InputState 不走 WS」的教訓）。

### Combat/hit detection — two separate implementations

Main game: `classes/Hurtbox.gd` (Area2D) just emits a `hurt(hitbox)` signal; `classes/Hitbox.gd` (Area2D) owns damage/knockback/poise, multi-hit tracking (`hit_targets`), and dispatches spark/shake/audio via the `CombatManager`/`AudioManager` autoloads before calling into the hurtbox. `classes/Damage.gd` is the payload (type LIGHT/HEAVY/THROW/NO_STUN; source_type MELEE/PROJECTILE/ASSIST).

VsMods: `VsMods/combat/VsHitbox.gd` (Area2D) has fighting-game-specific fields (`damage`, `hitstun_time`, `knockback`, `causes_knockdown`, `guard_break`). `VsMods/combat/VsHurtbox.gd` emits `hurt(hitbox)` signal received by `VsPlayer._on_hurtbox_hurt()`. No code is shared between the main-game and VsMods hit systems — know which mode you're editing before touching either.

### Armor / hyperarmor defense system (main game only)

Player side: `Player.gd` has a 3-tier `ArmorTier` enum (`NONE` / `HYPER_ARMOR` / `STRONG_HYPER_ARMOR`). `HYPER_ARMOR` blocks LIGHT hits only (no stagger, full damage); `STRONG_HYPER_ARMOR` blocks LIGHT+HEAVY with a 50% damage reduction. Only `Spear.gd` currently overrides `Weapon.get_armor_tier()` (its 4 收槍強化技 report `STRONG_HYPER_ARMOR`) — Katana/Sickle/Talisman don't yet; a weapon that wants tiered armor for a specific `combo_step` should override `get_armor_tier()` the same way, not invent a parallel flag. `Player.is_in_hitstun()` (state is Hurt/Launched) is a separate immunity path from `invincible_time_left` — both are checked wherever damage is gated.

**Player invincibility has three call-site patterns — they look redundant but are each pinned to a different `Engine.time_scale` situation, don't collapse them:**
1. `grant_invincibility(duration)` — the default helper; sets `invincible_time_left` **and** starts the `invincible_timer` Timer node. Use this for anything at normal game speed: `Hurt.gd`/`Launched.gd`'s post-hitstun grace window, `Guard.gd`'s chain-lock window, `Art_Katana_1.gd`'s counter.
2. Writing `player.invincible_time_left` directly, skipping `invincible_timer` entirely — used by every weapon ultimate (`Spear.gd`/`Katana.gd`/`Sickle.gd`/`Talisman.gd`'s `start_ultimate()`, `Art_Katana_6.gd`, `Art_Talisman_20.gd`). These all pair with `trigger_time_stop(_, 0.001)` (near-zero time scale) — a `Timer` node's countdown is itself slowed by `Engine.time_scale`, so at `0.001` a 3-second Timer-based invincibility would take ~3000 real seconds to expire. `invincible_time_left` is decremented with `unscaled_delta` in `Player.gd`, so it stays a true real-time duration regardless of time-stop.
3. Calling `invincible_timer.start(duration)` directly, skipping the float — used only by `Slide.gd::trigger_perfect_dodge()` (極限閃避), which does a **partial** slowdown (`time_scale = 0.3`), not a stop. Here the Timer's time-scale sensitivity is the wanted behavior: the 0.5s invincibility stretches proportionally with the bullet-time so it keeps covering the whole slow-motion window, instead of expiring in real time before the slowdown visually ends.

Before adding a new time-manipulating skill, decide which of these two axes it's on (full time-stop vs. partial slowdown vs. normal speed) and use the matching pattern — don't reflexively route everything through `grant_invincibility()`.

Enemy side (`enemies/enemy.gd`): two independent bools, `is_hyper_armored` (blocks player-inflicted stagger/knockback, not damage) and `is_invincible` (blocks everything), plus `Tier { LOW, MID, HIGH }`. **Convention:** any enemy attack state that should be hyperarmored calls `enter_attack_state()` on entry and `exit_attack_state()` on exit (it internally checks `tier == MID or HIGH` — LOW-tier enemies get no armor). Don't hand-roll `grant_hyper_armor()`/`clear_hyper_armor()` calls inside individual attack states; wrap the whole state's `enter()`/`exit()` instead so armor duration always matches the state's actual lifetime.

The **only** mechanism that pierces a defender's armor/guard is `hitbox.requires_countermeasure = true` (paired with the "yellow-ring" `start_guard_window()`/`try_guard_break()` flow on `Enemy.gd`) — before adding a new "breaks hyperarmor" field or system, check whether `requires_countermeasure` already covers it.

Status outline VFX (`StatusOutline.gdshader`, wired on both `Player.gd` and `Enemy.gd`) renders armor/invincibility as a colored edge glow. Both scripts resolve the sprite's live `.material` through a single function (`_resolve_main_sprite_material()`/`_refresh_main_sprite_material()` on `Enemy.gd`) rather than having the white-hit-flash and the outline effect each independently snapshot/restore `sprite.material` — the two used to race and leave sprites stuck fully white. Any new effect that touches the main body sprite's material must go through that same resolver, not add its own snapshot/restore.

`Enemy.gd::show_attack_warning(icon, delay, duration, fade_duration)` / `hide_attack_warning(icon)` is a generic, self-contained "驚嘆號" telegraph VFX utility — call it directly at the moment you want the icon to appear (it handles its own fade in/out and cancellation via an internal token), don't build a dedicated state/flow just to time a warning icon.

### Global autoloads (declared in `project.godot`)

| Autoload | Script | Responsibility |
|---|---|---|
| `Game` | `globals/game.gd` (`GameManager`) | Settings persistence (ConfigFile), fullscreen, custom keybindings, cross-scene world/player stats |
| `CombatManager` | `Explod/CombatManager.gd` | `Engine.time_scale` arbitration (domain slowdown/time-stop vs. UI pause — no hitstop, the game deliberately doesn't use it), camera shake, hit-spark VFX spawning |
| `AudioManager` | `sound/AudioManager.gd` | SFX/hit-SFX playback (`play_sfx`, `play_hit_sfx`) |
| `VsGameManager` | `VsMods/ui/VsGameManager.gd` | Cross-scene P1/P2 character/arts/arena selection cache for VsMods (`p1_character`/`p1_arts`/`selected_arena_id`/etc.) |
| `Vignette`, `PauseMenu` | scene autoloads | Screen vignette effect; pause UI |

Hitbox/Weapon code guards autoload calls with `has_method()` checks — treat this as the existing convention when adding new cross-autoload calls, not as something to "clean up".

### UI theme (`ui/`)

`ui/PixelTheme.tres` is the project-wide default `Theme` (wired via `project.godot`'s `[gui] theme/custom`), giving every menu Control (Panel/Button/CheckButton/OptionButton/HSlider/ScrollBar) a flat black-and-white pixel-line look — no rounded corners, no anti-aliasing, hover/pressed states invert to a white fill with black text. `Panel` (translucent, alpha 0.75 — full-screen menu backgrounds like `pause_menu.tscn`/`settings_panel.tscn`) and `PanelContainer` (fully opaque — `PixelOptionButton`'s dropdown list) are deliberately two separate StyleBoxFlats; don't collapse them back into one, they're translucent/opaque for different reasons. `ui/icons/` holds small generated pixel icons (checkbox, dropdown arrow, slider grabber) that back this theme.

**Don't use engine-native `OptionButton`/`PopupMenu` dropdowns anywhere in the main game UI.** At this project's base resolution (384x216, stretched via `canvas_items` mode), a native Popup is a separate `Window` that doesn't share the main viewport's stretch/oversampling, so its text renders visibly blurrier than everything else on screen — confirmed via screenshot, and there is no simple Theme/property fix (`Window.content_scale_factor` does **not** fix it — it just scales layout size, not render fidelity). Use `ui/PixelOptionButton.gd`/`.tscn` instead: a custom dropdown that opens a plain overlay of `Button` nodes (via a `CanvasLayer`, so it isn't clipped by parent `ScrollContainer`s) instead of a native Popup, staying inside the normal embedded-Control rendering pipeline that's guaranteed crisp. Its API deliberately mirrors `OptionButton` (`clear`/`add_item`/`set_item_metadata`/`get_item_metadata`/`selected`/`select`/`item_count`/`disabled`/`item_selected` signal) so it's close to a drop-in replacement. `VsMods/ui/select.tscn` still uses a native OptionButton — that's the separate VsMods framework and is out of scope for this fix.

## Conventions to know before editing

- Comments are bilingual (Traditional Chinese + English) with emoji section banners (`# ==...==`) — match this style in files that already use it.
- Player/weapon code frequently uses duck-typed `.get("property_name")` lookups instead of typed references — this is intentional loose coupling, not an oversight to "fix" with static typing.
- `PixelBaker/PixelBaker.gd.gd` has a doubled extension — this is the actual filename on disk, not a typo to silently rename.
- `Object.get_meta(key, default)` does **not** suppress the "meta not found" error when `default` is `null` — Godot can't distinguish "no default given" from "default explicitly `null`". Check `has_meta(key)` first instead of relying on a `null` default.
- A `Control` positioned via anchors+offsets under a plain (non-`Container`) parent does **not** auto-shrink to its children's content, even if the control itself is a `Container` type (e.g. `PanelContainer`) — its size is whatever the offsets say. If it needs to hug dynamically-changing content (e.g. a row of icons whose count varies), call `reset_size()` manually whenever that content changes.
